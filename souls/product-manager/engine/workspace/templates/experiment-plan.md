# A/B 实验方案：《<实验名>》

> 判定规则、指标、样本量、周期必须**事前**写死。方法见 `knowledge/product-playbook.md` §5，样本量速查见 `knowledge/benchmarks.md` §2。

| 字段 | 内容 |
| --- | --- |
| 负责人 / 日期 | <…> |
| 关联目标 | <北极星 / 业务目标> |

## 1. 假设

```
如果 <改动>，那么 <主指标> 会 <方向 + 量级>，因为 <机制>。
```

## 2. 分组设计

- **对照组（A）**：现状。
- **实验组（B）**：<改动>。
- **分流**：<比例，如 50/50>；**分流单位**：<**门店(company_uuid)** / 收银会话 / 订单>；**命中范围**：<哪些门店/终端进入实验>。
- **聚类提醒**：同店订单相关——按门店随机时**有效样本是门店数**，订单级 n 会高估功效（见 benchmarks §3）。

## 3. 指标

| 类型 | 指标 | 口径（分子/分母/时间窗/数据源表/时区） | 目标 / 阈值 |
| --- | --- | --- | --- |
| 主指标（唯一） | <如开台→送厨 duration_ms 中位数 / 自助下单占比> | <…，含 `ttpos_order_operation_duration` 等> | <达到 X 显著提升> |
| 护栏 1 | <退单率 / 支付失败率 / 客单价 / 出品时长> | <…> | <不显著恶化> |
| 护栏 2 | <客诉 / 崩溃率> | <…> | <…> |
| 次级（仅理解，不判定） | <…> | <…> | — |

## 4. 样本量与周期

- 基线 p = <…>（来源：门店历史数据）；MDE = <相对/绝对>；α = 0.05；power = 0.80。
- 每组样本量 = <计算 / 计算器结果>（速算 `n ≈ 16·p(1−p)/(绝对提升)²`；若按门店聚类，n 指**门店数**）。
- 日均命中流量 = <…> → **周期 = 每组样本×2 / 日均命中** ≈ <…>（至少跨完整营业周期，含周末；泰国节假日如宋干节单独标注）。

## 5. 判定规则（事前定，不可改）

- 达到样本量 **且** 周期满后才看结果（避免窥视）。
- **上线**：主指标显著提升且方向正确 **且** 所有护栏指标未显著恶化。
- **不上线 / 迭代**：主指标不显著或护栏恶化。
- 开始前确认分流比例符合预期（SRM 检查）。

## 6. 风险与回滚

- 风险：<新奇效应 / 季节性 / 埋点缺口>。
- 回滚：<异常触发条件 + 一键回滚开关>。
