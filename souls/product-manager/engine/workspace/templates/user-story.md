# 用户故事 + 验收标准（TTPOS）

> 一条故事一张卡。AC 每条可测（≥3 条满足质量门 verifiable_items）。大故事按 INVEST 拆小。
> AC 必须覆盖 **多终端差异**（同能力在 pos vs kiosk 行为常不同）与 **多租户隔离**（shop{company_uuid} 不串店）。

## US-<编号>：<一句话标题，无方括号前缀>

**角色 × 终端**：<角色>（<pos/kiosk/shop/...>）

**故事**
```
作为 <门店角色>，
我希望 <能力>，
以便 <经营价值 / 要完成的 Job>。
```

**验收标准（AC）**

| # | Given（前置） | When（动作） | Then（可观察结果，量化判据） |
| --- | --- | --- | --- |
| AC1 | <状态> | <角色操作> | <系统结果，含量化要求> |
| AC2 | <边界 / 空状态> | <操作> | <异常处理> |
| AC3 | <错误场景，如支付回调失败/弱网离线> | <操作> | <错误提示 / 兜底 / 重试> |
| AC4 | <多终端或多租户差异> | <操作> | <该终端/该租户的预期，与其它不同处> |

**优先级**：Priority = <P0/P1/P2/P3>（RICE/Kano 依据 <…>）/ Kano 类型 = <Must-be / Performance / Attractive> / SP = <研发估点>

**备注 / 假设 / 依赖**
- 假设：<待验证猜想 + 验证方式（交 experiment-design）>
- 依赖：<上下游服务 / 支付渠道 / 外卖平台>
- 数据口径：<本故事关联的经营指标 + 数据源表>
- 多租户/版本：<shop 库 or saas 库 / 旧版本 app 兼容>。
