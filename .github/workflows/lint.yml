name: lint

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  lint:
    runs-on: ubuntu-latest
    env:
      NODE_OPTIONS: --max-old-space-size=1024
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v4
        with:
          node-version: '24'

      - uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Lint
        run: bun run lint

      - name: Web lint
        run: bun run --filter '@zonease/aiworker-web' lint

      - name: Web test
        run: bun run --filter '@zonease/aiworker-web' test

      - name: Web build
        run: bun run --filter '@zonease/aiworker-web' build

      - name: Web shared cycle check
        run: bun run --filter '@zonease/aiworker-web' check:shared-cycles

      - name: Web bundle size report
        run: bun run --filter '@zonease/aiworker-web' size:report
