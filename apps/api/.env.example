# Brain provider selection: hermes | cloud-gateway
BRAIN_PROVIDER=hermes

# Hermes (Brain) — used when BRAIN_PROVIDER=hermes
HERMES_API_URL=http://localhost:8642
HERMES_HOME=~/.hermes

# Cloud Gateway (Brain via MCP) — used when BRAIN_PROVIDER=cloud-gateway
CLOUD_GATEWAY_MCP_URL=
CLOUD_GATEWAY_MCP_TOKEN=
# Default category or type_id for writeMemory when caller does not specify
CLOUD_GATEWAY_DEFAULT_CATEGORY=
CLOUD_GATEWAY_DEFAULT_TYPE_ID=

# OpenAI-compatible executor
OPENAI_BASE_URL=https://api.openai.com
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
OPENAI_TIMEOUT_MS=60000

# Legacy (deprecated — kept in schema for transitional compatibility)
# OPENCLAW_WS_URL=ws://localhost:18789
# OPENCLAW_HOME=~/.openclaw

# Server
PORT=3001
