# REL-003 发布 aiworker CLI 0.4.6

- **status**: in_progress
- **priority**: P1
- **owner**: self
- **createdAt**: 2026-04-29 17:50

## Description

发布 `@zonease/aiworker-cli@0.4.6`，作为 `0.4.5` 之后的 patch release。
本次发版包含已合入 main 的 CLI 生命周期修复、Codex executor reconnect 容忍修复、
项目工作指引刷新，以及按 `DESIGN.md` 收敛后的 Fleet / Worker Web UI。

Release checklist:

1. 将 CLI package 版本从 `0.4.5` bump 到 `0.4.6`。
2. 跑完本地 release quality gates。
3. 构建 publish artifact，并确认 dist manifest 报告 `0.4.6`。
4. 通过现有 tag-triggered GitHub release workflow 发布。
5. 验证 npm `latest` 解析到 `0.4.6`，并验证已发布 CLI 版本。

## ActiveForm

发布 aiworker CLI 0.4.6

## Dependencies

- **blocked by**: none
- **blocks**: operators installing the latest CLI/Web UI fixes from npm
- **relates to**: BUG-035, BUG-036, DOC-001, REFACTOR-012, PLAN-040

## Notes

- 2026-04-29 17:50 npm registry check: `@zonease/aiworker-cli` latest is
  `0.4.5`; `0.4.6` is not published yet.
- Release source remains `apps/cli/dist`; npm publish is expected to run from
  the tag-triggered GitHub workflow rather than a local publish command.
- 2026-04-29 17:54 Local release gates passed. `apps/cli/dist/package.json`
  reports `0.4.6`, Fleet and Worker Web bundles are present in dist, and
  `bun publish --dry-run --access public` packed 25 files before stopping at
  the expected local npm authentication boundary.
