# BUG-051 Preserve hyphenated executor MCP arg values

- **status**: completed
- **priority**: P3
- **owner**: Codex
- **createdAt**: 2026-05-03 23:33
- **discoveredAt**: 2026-05-03 22:28
- **plan**: TBD

## DOC-006 framing

This remains a valid low-priority executor overlay UX defect, but it is no
longer tied to rejected `PLAN-080`. After DOC-005, it belongs in the
onboarding polish lane after the P1 truthfulness, admission governance bridge,
and executor parity work.

## Observed Behavior

`aiworker executor mcp add` supports repeated `--arg <value>` for stdio MCP
commands. Passing a hyphenated command argument as two tokens fails:

```bash
aiworker executor mcp add filesystem-probe \
  --engine codex \
  --transport stdio \
  --command npx \
  --arg -y
```

The CLI reports:

```text
ERROR Unknown option -y
```

The workaround is to use equals form:

```bash
--arg=-y
```

## Expected Behavior

Operators should be able to pass common stdio command arguments such as `-y`
without discovering parser-specific escaping rules by trial and error, or the
help/error output should clearly tell them to use `--arg=-y` / `--arg -- -y`.

## Acceptance Criteria

1. Decide whether to support `--arg -y` directly or document/enforce a clear
   passthrough form.
2. Keep existing repeated `--arg=value` behavior.
3. Add focused CLI coverage for hyphenated stdio MCP arguments.
4. Update command help or docs if the supported syntax remains equals-only.
5. Ensure the fix does not loosen unknown-option validation for the rest of
   the CLI.

## ActiveForm

Improving executor MCP stdio argument UX for hyphenated values.

## Dependencies

- **blocked by**: none
- **blocks**: smoother executor MCP onboarding
- **relates to**: FEAT-044, FEAT-047, PLAN-075, QA-003, PLAN-080

## Notes

- This is not a release blocker because `--arg=-y` works and current docs can
  use the equals form.
- The parser behavior may live at the CLI option preprocessing boundary rather
  than in `runExecutorMcpAdd()` itself.

## Progress

- 2026-05-06 02:40：PLAN-120 已 claim。将只在 executor mcp add 命令预处理
  `--arg <hyphenated>`，不放宽其它 CLI unknown option validation。
- 2026-05-06 03:05：完成。`preprocessArgv` 只对 `executor mcp add` /
  `worker executor mcp add` 把 `--arg -y` 转成 `--arg=-y`；focused test 断言 manifest
  中 args 保留 `-y`，其它 unknown option validation 不变。
