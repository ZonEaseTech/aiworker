# QA-028 Soul project MVP validation

- **status**: completed
- **priority**: P0
- **owner**: local
- **createdAt**: 2026-05-10 11:26
- **plan**: PLAN-215
- **relatesTo**: REFACTOR-045

## Goal

Validate that the corrected MVP is usable from a clean local preview:

1. Open Web and see the OD-style AIWorker Soul workspace, not a dashboard, work
   order center, import tool, or demo shell.
2. Select HR, PM, QA, and DevOps Souls and see scoped capability templates.
3. Create a Soul project, start a run, and preview the generated business
   artifact.
4. Open Settings, change values, save, close, reload, and confirm persistence.
5. Confirm no visible `project`, import entry, work-order-first entry,
   Open Design/Nexu copy, design-generation language, or default legacy
   `.aiworker/local`/scope/capability JSON artifacts remain in the product path.

## Acceptance Criteria

- Focused Web/API/core/shared/storage/CLI/fs-layout tests pass.
- Focused Web/API/core/shared/storage/CLI/fs-layout typecheck/build gates pass
  where applicable.
- Browser validation captures corrected home, artifact preview, and Settings.
- code-review-graph runs after code edits and findings are recorded.

## Evidence

- Focused gates passed:
  `@zonease/aiworker-shared typecheck`,
  `@zonease/aiworker-storage-sqlite typecheck/test`,
  `@zonease/aiworker-core typecheck/test`,
  `@zonease/aiworker-api typecheck/test/build`,
  `@zonease/aiworker-cli typecheck/test/build:bundle`,
  `@zonease/aiworker-web typecheck/test/lint/build`,
  `@zonease/aiworker-fs-layout typecheck/test`,
  `bun run db:generate:worker`.
- Root gates passed: `bun run typecheck`, `bun run test`, `bun run build`,
  `bun run lint`, and `git diff --check`. Lint exits 0 with 5 existing
  `react-hooks-extra/no-direct-set-state-in-use-effect` warnings in
  `apps/web/src/worker/worker-studio.tsx`.
- Browser validation passed at `http://127.0.0.1:5178/worker/`: HR, PM, QA, and
  DevOps Souls each produced one project, run, and artifact; Settings Test and
  Rescan completed; language changed to `zh-CN` and persisted after reload;
  console error count was 0.
- Browser data check returned `projectCount=4`, `runCount=4`,
  `artifactCount=4`, `soulIds=[devops, hr, pm, qa]`, `bodyHasCase=false`, and
  `bodyHasImport=false`.
- code-review-graph update passed; `detect-changes` reported 32 changed files,
  116 changed symbols, 0 affected flows, 81 test gaps, and risk score 0.60.
  `get_minimal_context` reported medium risk; `get_impact_radius` reported high
  blast radius because this is an intentional cross-layer rewrite.
