# QA-023 Worker Case source dogfood and packaged validation

- **status**: completed
- **priority**: P1
- **owner**: local
- **createdAt**: 2026-05-09 06:55
- **completedAt**: 2026-05-09 12:11
- **relatesTo**: FEAT-057, PLAN-189, REL-030

## Context

FEAT-057 turns the proof-loop primitives into the Worker Case operating surface.
Validation must prove the flow is useful to an operator, not only that REST and
CLI endpoints return JSON.

## Scope

- Source gates: typecheck, lint, tests, build.
- Case projection tests across core/API/CLI/Web/gateway bridge.
- CLI bundle smoke for `case list/show/rerun` and `lessons propose` command
  registration.
- Source dogfood against real worker-owned task rows where possible.
- Published/local-package validation after REL-030 release.

## Evidence

- 2026-05-09 11:57：source gates 通过：`bun run check`、`bun run test`、
  `bun run build`、`git diff --check`。
- 2026-05-09 11:57：Case projection 覆盖已进入完整测试：core `BrainCaseService`、
  Worker REST/OpenAPI、CLI `case` commands、Worker Admin `/cases`、gateway
  `cases.*` bridge 与 worker bridge allowlist。
- 2026-05-09 11:57：bundle CLI dogfood 使用
  `/tmp/aiworker-case-dogfood-39ezZQ/project`。`case list` 返回 `ready_to_ship`
  Case File，`case show` 保留 redacted prompt 与 evidence refs，`lessons propose`
  创建 pending `memory-add` proposal 并回填完整 `proposalIds`。
- 2026-05-09 11:57：dogfood 发现 `task-case` proposal id 会被 secret scanner 误判；
  已收窄 `sk-` token 规则与 high-entropy fallback，新增回归测试并以
  `2a8d194 fix: 避免 redactor 误截断 Case proposal id` 提交。
- 2026-05-09 12:11：发布后验证通过：npm latest 为 `0.12.0`，
  `bunx @zonease/aiworker-cli@0.12.0 --version` 返回 `aiworker/0.12.0`。
- 2026-05-09 12:11：`v0.12.0` release workflow `25591091932` 通过，GitHub
  Release assets 完整，main lint/build-image workflows 通过。
- 2026-05-09 12:11：发布包 compact governance harness 通过，覆盖
  `developer-codex` 与 `general-assistant-claude-code`，合计 80 PASS / 0 FAIL。

## Residual risk

- PLAN-187 batch Lessons Queue review is intentionally not implemented in this
  release; the safe path remains per-case lesson proposal followed by Brain
  admission approve/reject/apply.
