# REL-014 发布 aiworker CLI 0.7.0

- **status**: in_progress
- **priority**: P1
- **owner**: aiworker-maintainer
- **createdAt**: 2026-05-05 02:30
- **claimedAt**: 2026-05-05 02:30
- **plan**: PLAN-108

## 描述

发布 `@zonease/aiworker-cli@0.7.0`，作为 `0.6.0` 之后的 minor release，交付 QA-004（0.6.0 published claude-code Soul/Brain 端到端调试）登记的 9 个 task 通过 PLAN-105 / PLAN-106 / PLAN-107 三个 plan 的修复，包括 P0 安全 / P0 产品定位 / P1 brain LLM evaluator 等关键缺陷。

本次发版包含：

- `PLAN-105` Project Brain 注入贯穿 4 个 executor adapter（claude-code / codex / acp / cursor）+ orchestrator decision retry。覆盖 `BUG-056` (P0 产品定位) / `BUG-057` (P1 brain LLM evaluator JSON.parse)。
- `PLAN-106` Brain admission MVP 安全 / 鲁棒 / 可观察性补齐：secret scan 三档 policy（block / redact / raw）+ per-row safeParse + unsupported kind audit + REST `POST /admission`（仅 `WORKER_DEV_TOOLS=true`）+ CLI `aiworker brain admission propose`。覆盖 `BUG-055` (P0 安全) / `BUG-058` (P2 schema drift) / `BUG-059` (P3 状态机) / `TODO-009` (P3) / `TODO-010` (P3)。
- `PLAN-107` CLI `brain brief --artifact` 兜底 + `aiworker init` next-steps 文案修复。覆盖 `BUG-054` (P2) / `TODO-011` (P3)。
- `chore(tsconfig)` 删除 workspace tsconfig baseUrl 兼容 TypeScript 7.0。

## ActiveForm

发布 aiworker CLI 0.7.0

## Dependencies

- **blocked by**: none
- **blocks**: published CLI 用户拿到 PLAN-105/106/107 的 brain 注入 + admission 安全 + brief / init 文案修复
- **relates to**: PLAN-105, PLAN-106, PLAN-107, PLAN-108, QA-004, BUG-054, BUG-055, BUG-056, BUG-057, BUG-058, BUG-059, TODO-009, TODO-010, TODO-011

## Notes

- 2026-05-05 02:30：npm latest、GitHub Release 和远端 tag 当前均为 `0.6.0`；本次按 `0.7.0` minor release 执行（BUG-056 修通后 SOUL/AGENT/MEMORY 真正注入 LLM，PLAN-106 新增 CLI/REST surface，按 semver 0.x 走 minor）。
- 自 `v0.6.0` 至 release commit 前共 9 个 commit，其中 3 个 PLAN（105/106/107）+ docs 收口 + tsconfig baseUrl 兼容 + npm description polish + serena project 维护。

## Validation

- `bun run typecheck`
- `bun run lint`
- `bun run test`
- `bun run build`
- `git diff --check`
- `apps/cli/dist/package.json` 版本字段 = `0.7.0`
- GitHub Actions release workflow 全绿
- `npm view @zonease/aiworker-cli version` → `0.7.0`
- `gh release view v0.7.0` → 4 个平台 binary uploaded
