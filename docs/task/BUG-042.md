# BUG-042 `aiworker up --dry-run` prints `NaN` for omitted port

- **status**: completed
- **priority**: P3
- **owner**: local
- **createdAt**: 2026-05-02 19:51
- **discoveredAt**: 2026-05-02 19:49
- **claimedAt**: 2026-05-03 10:57
- **completedAt**: 2026-05-03 10:59

## Observed Behavior

During the published `@zonease/aiworker-cli@0.5.0` smoke test, `aiworker up --soul developer --dry-run --no-open --no-serve-web` completed successfully but printed:

```text
port         : NaN
```

This only affects dry-run display output when `--port` is omitted.

## Expected Behavior

Dry-run output should show the effective environment/default port or an explicit placeholder such as `(env/default)`, never `NaN`.

## Reproduction

```bash
bunx --bun --package @zonease/aiworker-cli@0.5.0 aiworker up --soul developer --dry-run --no-open --no-serve-web
```

## Acceptance Criteria

1. `aiworker up --dry-run` without `--port` does not print `NaN`.
2. Existing `up` dry-run tests cover omitted port and explicit port output.
3. No serve behavior changes.

## Notes

- 2026-05-03 10:57 Claimed for the pre-0.5.1 release cleanup.
- 2026-05-03 10:59 Fixed by formatting omitted dry-run port as
  `(env/default)` while preserving explicit port output.
- 2026-05-03 11:18 Published `0.5.1` smoke still reproduced `NaN`; root cause
  was the CLI command layer passing CAC's omitted numeric option as `[NaN]`.
  Fixed by normalizing optional numeric option arrays before calling command
  handlers and adding CLI-entrypoint integration coverage.

## Validation

- `bun test apps/cli/src/commands/worker/up.test.ts`
- `bun test apps/cli/src/commands/worker/up.integration.test.ts apps/cli/src/commands/worker/up.test.ts apps/cli/src/aiworker.test.ts`
- Manual CLI entrypoint check: `bun apps/cli/src/aiworker.ts up --soul developer --dry-run --no-open --no-serve-web`
- `bun run --filter '@zonease/aiworker-cli' typecheck`
