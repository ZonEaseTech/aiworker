# REL-001 Publish aiworker CLI 0.4.4

- **status**: completed
- **priority**: P1
- **owner**: Codex
- **createdAt**: 2026-04-28 19:14

## Description

Publish `@zonease/aiworker-cli@0.4.4` as the patch release after the 0.4.3
test-server validation found and the repository fixed two release-worthy
regressions:

1. `BUG-027`: accepted gateway chat conversation ids must be reusable for
   continuation.
2. `BUG-028`: packaged Web UI CSS must include Tailwind utilities.

Release checklist:

1. Bump the CLI package version from `0.4.3` to `0.4.4`.
2. Run local release quality gates.
3. Build the publish artifact and verify the dist manifest reports `0.4.4`.
4. Publish through the existing tag-triggered GitHub release workflow, or fall
   back to local publishing only when npm authentication is available.
5. Verify npm `latest` resolves to `0.4.4`.

## ActiveForm

Publishing aiworker CLI 0.4.4

## Dependencies

- **blocked by**: none
- **blocks**: test-server upgrade to the fixed 0.4.4 CLI package
- **relates to**: BUG-027, BUG-028, FEAT-027

## Notes

- 2026-04-28 19:14 npm registry check: `@zonease/aiworker-cli` latest is
  `0.4.3`; `0.4.4` is not published yet.
- Local npm auth is not available in this shell, so the preferred path is the
  existing tag-triggered GitHub release workflow with repository secrets.
- 2026-04-28 19:19 Published `@zonease/aiworker-cli@0.4.4` through the
  tag-triggered GitHub release workflow. npm `latest` now resolves to `0.4.4`,
  `bunx @zonease/aiworker-cli@0.4.4 --version` reports `aiworker/0.4.4`, and
  GitHub Release `v0.4.4` contains the four platform tarballs.
