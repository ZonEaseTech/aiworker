# QA-008 0.9.1 published CLI `cli-release-local` validation

- **status**: completed
- **priority**: P1
- **owner**: local
- **createdAt**: 2026-05-06 05:58
- **claimedAt**: 2026-05-06 05:58
- **completedAt**: 2026-05-06 05:58
- **plan**: PLAN-126
- **sourceObjective**: `aiworker-validate cli-release-local 0.9.1`
- **relatesTo**: REL-017, PLAN-124, TODO-030, PLAN-125

## Scope

Run the canonical `aiworker-validate` `cli-release-local` mode against the
already-published `@zonease/aiworker-cli@0.9.1` package.

Evidence root:

- `/home/ben/projects/debug-aiworker/qa-2026-05-06-cli-0.9.1`

Product under test:

- Published npm package: `@zonease/aiworker-cli@0.9.1`.
- Isolated install path:
  `/home/ben/projects/debug-aiworker/qa-2026-05-06-cli-0.9.1/bin/node_modules/@zonease/aiworker-cli`.
- CLI version output:
  `aiworker/0.9.1 linux-x64 node-v24.3.0`.

## Non-Scope

- Did not validate shared fleet, gateway enrollment, Fleet UI, or remote
  worker attachment; this was not `fleet-remote`.
- Did not run source checkout builds, source dev servers, or workspace CLI as
  the product under test.
- Did not run the full 5 Soul x 2 executor governance campaign; this was a
  compact post-release local black-box validation.
- Did not persist bearer tokens, bootstrap tokens, master keys, cookies, auth
  headers, or raw executor prompts into PMA docs.

## Validation Matrix

| Surface | Result | Evidence |
|---------|--------|----------|
| Published package install | PASS | `logs/completion-audit.txt`; `logs/aiw-version.txt` |
| `developer` + `codex/default` init / doctor / brain status | PASS | `logs/init-developer-codex.log` |
| `general-assistant` + `claude-code/default` init / doctor / brain status | PASS | `logs/init-general-assistant-claude-code.log` |
| `developer` + `codex/default` single run | PASS | `samples/dev-codex-smoke.log` |
| `developer` + `codex/default` 8-turn continuity run | PASS | `samples/dev-codex-turn-*.log` |
| `general-assistant` + `claude-code/default` single run | PASS | `samples/general-claude-smoke.log` |
| DB / filesystem assertions | PASS | `logs/db-filesystem-codex.txt` |
| Worker REST / OpenAPI / SSE smoke | PASS | `logs/rest-smoke.txt`; `logs/rest-routes-events.txt`; `logs/rest-conversation-messages.txt` |
| Serve cleanup | PASS | `logs/cleanup.txt` |

## Findings

No new product defect was confirmed.

Positive evidence:

- The published package resolved to `0.9.1`, and the isolated binary reported
  `aiworker/0.9.1 linux-x64 node-v24.3.0`.
- Both fresh project scopes initialized correctly, selected the requested
  executor, and passed `aiworker executor doctor`, `aiworker doctor`, and
  `aiworker brain status`.
- The 8-turn `developer` + `codex/default` run kept one stable chat id and
  completed all turns with `orchestrator.finished`.
- The multi-turn run covered identity/scope, file create/read/modify, marker
  recall, high-risk production DB refusal, admission proposal behavior,
  ambiguous prompt handling, and final consistency recap.
- Filesystem and DB verification matched runtime claims:
  `notes/cli091.txt` contained the expected marker and modification line;
  one conversation row and 16 messages were present for the multi-turn chat;
  two `memory-add` admission proposals were present with `pending` status;
  no Project Brain memory file was written directly.
- `general-assistant` + `claude-code/default` completed a real run and
  explained that long-term preferences must go through Project Brain
  admission instead of executor-native memory.
- `serve` exposed `runtimeVersion: "0.9.1"`, returned 401 for unauthenticated
  `/api/worker/info`, returned authenticated worker info and brain summary,
  exposed 14 OpenAPI paths including task/conversation/event routes, and
  established the SSE stream.
- The serve process was terminated and port `19391` was released.

## Validation

- `npm view @zonease/aiworker-cli@0.9.1 version` -> `0.9.1`.
- Isolated `aiworker --version` -> `aiworker/0.9.1 linux-x64 node-v24.3.0`.
- `aiworker init --soul developer` + `aiworker executor select --engine codex --apply`.
- `aiworker init --soul general-assistant` + `aiworker executor select --engine claude-code --apply`.
- `aiworker executor doctor`, `aiworker doctor`, and `aiworker brain status` for both scopes.
- `aiworker run` single-turn and 8-turn multi-turn samples.
- SQLite assertions against `.aiworker/local/worker.db`.
- REST assertions against the published package `serve` process on loopback.
- `git diff --check`.
