# REL-012 发布 aiworker CLI 0.5.3

- **status**: completed
- **priority**: P1
- **owner**: aiworker-maintainer
- **createdAt**: 2026-05-03 21:33
- **claimedAt**: 2026-05-03 21:33
- **completedAt**: 2026-05-03 21:39
- **plan**: PLAN-079

## 描述

发布 `@zonease/aiworker-cli@0.5.3`，作为 `0.5.2` 之后的 patch release，交付当前 main 上的 worker brain/executor bootstrap lifecycle 更新和 pre-release UX polish。

本次发版包含：

- `FEAT-046` worker local brain activation and lifecycle。
- `FEAT-047` worker executor bootstrap and capability lifecycle。
- `BUG-049` user/explicit scope init next steps 不再提示 project-only executor doctor。

## ActiveForm

发布 aiworker CLI 0.5.3

## Dependencies

- **blocked by**: none
- **blocks**: published CLI users receiving the brain/executor bootstrap lifecycle updates
- **relates to**: FEAT-046, FEAT-047, BUG-049, PLAN-079

## Notes

- 2026-05-03 21:33：npm latest、GitHub Release 和远端 tag 当前均为 `0.5.2`；本次按 `0.5.3` patch release 执行。
- 2026-05-03 21:35：本地 release gates 通过：workspace tests、typecheck、lint、build、dist artifact check、built CLI `init --global` next-step smoke、built CLI `up --dry-run` smoke、CLI smoke scripts、publish dry-run pack 阶段、`git diff --check`。
- 2026-05-03 21:39：`v0.5.3` release workflow `25280654558` 成功；npm latest 解析到 `0.5.3`，GitHub Release 上传 linux-x64、linux-arm64、darwin-x64、darwin-arm64 tarballs；published CLI smoke 通过 `--version`、`init --global` next steps 和 `up --dry-run`。

## Validation

- `bun run test`
- `bun run typecheck`
- `bun run lint`
- `bun run build`
- `bun run --filter '@zonease/aiworker-cli' smoke:aiworker-run`
- `bun run --filter '@zonease/aiworker-cli' smoke:aiworker-fleet`
- built CLI `apps/cli/dist/aiworker.js --version`
- built CLI `init --global` next-step smoke
- built CLI `up --soul developer --dry-run --no-open --no-serve-web`
- `cd apps/cli/dist && bun publish --dry-run --access public` (passed to pack/auth boundary)
- `git diff --check`
- GitHub Actions release workflow `25280654558`
- `npm view @zonease/aiworker-cli version --json`
- `gh release view v0.5.3 --repo ZonEaseTech/aiworker`
- published CLI `bunx --bun --package @zonease/aiworker-cli@0.5.3 aiworker --version`
- published CLI `init --global` next-step smoke
- published CLI `up --soul developer --dry-run --no-open --no-serve-web`
