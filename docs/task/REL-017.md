# REL-017 发布 aiworker CLI 0.9.1

- **status**: completed
- **priority**: P1
- **owner**: local
- **createdAt**: 2026-05-06 04:16
- **claimedAt**: 2026-05-06 04:16
- **completedAt**: 2026-05-06 04:24
- **plan**: PLAN-124

## 描述

发布 `@zonease/aiworker-cli@0.9.1`，作为 `0.9.0` 之后的 patch release，交付 QA-007 / PLAN-123 登记并完成的 Brain Governance Kernel 后续修复。

本次发版包含：

- `BUG-075`：quality gate LLM evaluator 的 no-tools control prompt 回归覆盖，确保 user prompt 非空并包含 request + assistant answer。
- `TODO-028`：decision pipeline recent samples 写入 worker.db，CLI `run` 后独立 `brain status` 可读回最近窗口。
- `BUG-076`：suppressed control calls 显式 `tools: []`，Claude Code adapter best-effort 投影 no-tool flags 并拒绝工具控制请求。
- `BUG-077`：dead-loop detector 将 `tool_result` 和 terminal tool lifecycle status 视为 progress，避免合法多工具 turn 被误杀。
- `BUG-078`：Worker OpenAPI 移除 stale chat route，补齐 task / conversation 路由。
- `TODO-029`：bypass heuristic 收窄到高置信 mutation claim，并带脱敏 `claimExcerpt`。

## ActiveForm

发布 aiworker CLI 0.9.1

## Dependencies

- **blocked by**: none
- **blocks**: published CLI 用户拿到 QA-007 后续 Brain Governance Kernel 修复
- **relates to**: PLAN-123, PLAN-124, QA-007, BUG-075, TODO-028, BUG-076, BUG-077, BUG-078, TODO-029

## Notes

- 2026-05-06 04:16：npm latest、GitHub Release 和远端 tag 当前均为 `0.9.0`；远端不存在 `v0.9.1` tag。本次按 `0.9.1` patch release 执行：`0.9.0` 后仅 1 个 fix commit 和 1 个 release-closeout docs commit，主要是缺陷修复、观测持久化和 OpenAPI 文档修正。
- 2026-05-06 04:25：本地 release gates 全通过。`apps/cli/dist/package.json` 和 built CLI `--version` 均为 `0.9.1`；publish dry-run 完成 pack 阶段（32 files / 2.73 MB），随后停在本机 npm authentication boundary。
- 2026-05-06 04:24：release commit `a2a86e5 chore(release): 发布 CLI 0.9.1` + annotated tag `v0.9.1` 已 push 到 origin；GitHub Actions release workflow run id `25416301967` (job `74548401046`) 1m56s 全绿；npm `@zonease/aiworker-cli` `latest=0.9.1` 已上线；`bunx @zonease/aiworker-cli@0.9.1 --version` 返回 `aiworker/0.9.1 linux-x64 node-v24.3.0`；GitHub Release `v0.9.1` 已发布（非 draft / 非 prerelease），4 个平台 binary 全部 uploaded（darwin-arm64 23.96 MB / darwin-x64 26.38 MB / linux-arm64 39.66 MB / linux-x64 39.99 MB）。

## Validation

- `bun install --frozen-lockfile`
- `bun run typecheck`
- `bun run lint`
- `bun run test`
- `bun run build`
- `bun run --filter '@zonease/aiworker-cli' smoke:aiworker-run`
- `bun run --filter '@zonease/aiworker-cli' smoke:aiworker-fleet`
- `apps/cli/dist/package.json` 版本字段 = `0.9.1`
- `bun apps/cli/dist/aiworker-bun.js --version` → `aiworker/0.9.1 ...`
- `git diff --check`
- publish dry-run pack 阶段
- GitHub Actions release workflow `25416301967` 全绿
- `npm view @zonease/aiworker-cli version` → `0.9.1`
- `bunx @zonease/aiworker-cli@0.9.1 --version` → `aiworker/0.9.1 linux-x64 node-v24.3.0`
- `gh release view v0.9.1` → published, 4 个平台 binary uploaded
