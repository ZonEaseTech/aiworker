# REL-036 发布 aiworker CLI 0.15.1

- **status**: completed
- **priority**: P1
- **owner**: codex
- **createdAt**: 2026-05-15 13:57
- **claimedAt**: 2026-05-15 13:57
- **completedAt**: 2026-05-15 14:08
- **plan**: PLAN-327
- **relatesTo**: REL-035, v0.15.0

## 背景

`@zonease/aiworker-cli@0.15.0` 已经发布，npm latest 与 GitHub Release 均指向
`0.15.0`。`main` 在 `v0.15.0` 之后包含 Worker Web shell header action 修复：
Host 不再把 Soul App header action 成功消息渲染为主视觉状态行，避免 HR People
Workbench 点击 header button 后出现干扰性状态文案；失败消息仍以 alert 暴露。

该修复进入发布包中的 Worker Web static assets，适合作为 `0.15.1` patch release
发布。

## 范围

- 将 CLI package version 从 `0.15.0` bump 到 `0.15.1`。
- 运行本地 source/build/dist/pack/smoke release gates。
- 提交 release prep commit。
- 推送 `main` 与 annotated tag `v0.15.1`。
- 监控 tag-triggered GitHub Actions release workflow。
- 验证 npm latest、显式 package smoke、GitHub Release tarball/checksum assets
  与 published-package preview smoke。
- 更新本任务、`PLAN-327`、索引文件与 `docs/changelog.md` 的 release evidence。

## 非范围

- 不发布 `@zonease/aiworker-soul-app-sdk` 或
  `@zonease/aiworker-soul-app-runtime` 独立 npm 包。
- 不声明 1.0 GA。
- 不新增 Host auth、cloud marketplace、gateway/fleet 或远程控制面 scope。
- 不重写 `0.15.0` release tag 或 npm package。

## 验收标准

1. 本地 release gate 全部通过。
2. `apps/cli/package.json` 与 `apps/cli/dist/package.json` 均报告 `0.15.1`。
3. `npm pack --dry-run --json` 证明 CLI shims、Worker Web static、worker
   migrations 与官方 HR/QA app release resources 进入发布包。
4. `smoke:dist-release` 证明 dist CLI 能启动 Host Web/API、bootstrap official
   HR/QA Soul Apps，并能发现 HR template。
5. `main` 与 `v0.15.1` 推送成功。
6. GitHub Actions release workflow 成功。
7. npm latest 解析到 `0.15.1`。
8. `bunx @zonease/aiworker-cli@0.15.1 --version` 报告 `aiworker/0.15.1`。
9. GitHub Release `v0.15.1` 存在且含四个平台 binary tarballs 和四个
   matching `.sha256` checksum assets。
10. Published-package smoke 证明已发布 CLI 的 Host/Soul App preview path 可用。

## 记录

- 2026-05-15 13:57：用户要求发版。确认 npm latest 为 `0.15.0`，GitHub
  Release `v0.15.0` 存在且含四个平台 tarballs 与四个 `.sha256` checksum
  assets；远端 `v0.15.1` tag 尚未使用。开始执行 `0.15.1` patch release prep。
- 2026-05-15 14:02：本地 release gates 通过：`bun run check`、`bun run
  test`、`bun run build`、`git diff --check`、dist CLI `--version`、dist
  package version、`npm pack --dry-run --json`、`smoke:dist-release`。
  `apps/cli/dist/package.json` 与 dist CLI 均报告 `0.15.1`；pack dry-run
  输出 `@zonease/aiworker-cli@0.15.1`，119 个文件，包含 CLI shims、Worker
  Web static、worker migrations 与官方 HR/QA app resources。CRG 已运行，
  risk score `0.00`。
- 2026-05-15 14:04：第一次 tag-triggered release workflow
  `25903035166` 在 Test 阶段失败，尚未执行 npm publish 或 GitHub Release
  asset upload。根因是 WorkerStudio 回归测试在点击 `New people profile`
  后立即 `getByRole('dialog')`，CI 上 action 仍处于 busy 状态导致断言竞态；
  改为 `findByRole('dialog')` 等待 dialog 出现，同时保留成功状态行不渲染的断言。
- 2026-05-15 14:06：测试竞态修复后，本地 CI 语义验证通过：聚焦 WorkerStudio
  回归测试 1 passed / 29 skipped，`CI=true bun run test` 全量通过，`bun run
  check`、`git diff --check`、`bun run crg:update` 与 `bun run crg:review`
  通过。
- 2026-05-15 14:08：`main` 更新到 `424018c7`，远端 `v0.15.1` tag 已重建到
  该提交。第二次 release workflow `25903157643` 成功，完成 typecheck、test、
  bundle、npm publish、binary compile、tarball package 与 GitHub Release asset
  attach。npm latest 解析到 `0.15.1`；`bunx
  @zonease/aiworker-cli@0.15.1 --version` 报告 `aiworker/0.15.1
  darwin-arm64 node-v24.3.0`；GitHub Release `v0.15.1` 为 ready release，
  包含四个平台 tarballs 与四个 matching `.sha256` checksum assets。
  Published-package smoke 通过，验证已发布 CLI 可启动 Host Web/API、报告
  runtimeVersion `0.15.1`、bootstrap official HR/QA Soul Apps，并发现
  `aiworker-hr.person-profile` template。

## 残余风险

- GitHub Actions release workflow 仍可能报告既有
  `softprops/action-gh-release@v2` Node.js 20 deprecation annotation。该注解未阻塞
  `0.15.0` 发布，但仍应在后续 workflow 维护中处理。
