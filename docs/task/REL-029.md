# REL-029 发布 aiworker CLI 0.11.0

- **status**: in-progress
- **priority**: P1
- **owner**: local
- **createdAt**: 2026-05-09 08:28
- **claimedAt**: 2026-05-09 08:28
- **plan**: PLAN-182
- **sourceObjective**: 发布 FEAT-056 developer repo worker proof-loop source MVP，
  并用 published-package 验证确认 0.11.0 可作为 0.x minor 版本投产。
- **relatesTo**: FEAT-056, QA-022, REL-028

## 背景

`@zonease/aiworker-cli@0.10.4` 是当前 npm latest 与最新 GitHub Release。`v0.10.4..HEAD`
已包含 FEAT-056 的 proof-loop runtime 和 readiness 收口：

- Brain Journal 与 Gate verdict。
- Brain Engine no-tools reviewer。
- bounded rerun / hold lineage。
- Brain Inbox lesson admission flow。
- authority preflight risk warning。
- source-backed dogfood QA-022 与 readiness docs。

这批变更改变 worker proof-loop 的 operator surface，不应作为 patch 发布；本次按
minor release 发布 `0.11.0`，但不声明 1.0 GA。

## 范围

- 将 `apps/cli/package.json`、README latest 与相关发布文档更新到 `0.11.0`。
- 运行本地 release gate。
- 构建并检查 CLI dist 产物版本。
- 执行 publish dry-run 到本机 npm auth boundary。
- 提交 release bump。
- 推送 `main` 与 annotated tag `v0.11.0`。
- 监控 tag-triggered GitHub Actions release workflow。
- 验证 npm latest、显式 package smoke、GitHub Release assets。
- 运行发布包 `cli-release-local` compact harness。

## 非范围

- 不发布 1.0 GA。
- 不新增 runtime feature。
- 不改变 release workflow。
- 不做 fleet/gateway 远端升级。

## 验收标准

1. 本地 release gate 全部通过。
2. `main` 与 `v0.11.0` 推送成功。
3. GitHub Actions release workflow 成功。
4. npm latest 解析到 `0.11.0`。
5. `bunx @zonease/aiworker-cli@0.11.0 --version` 报告 `aiworker/0.11.0`。
6. GitHub Release `v0.11.0` 存在且含四个平台 binary assets。
7. 发布包 `cli-release-local` compact harness 通过，或记录明确 finding / blocker。

## 记录

- 2026-05-09 08:28：任务打开。确认 npm latest 为 `0.10.4`，本地 CLI package
  version 为 `0.10.4`，远端暂无 `v0.11.0` tag；发布目标为 `0.11.0` minor。
- 2026-05-09 08:31：本地 release gate 已通过：`bun install --frozen-lockfile`、
  `bun run typecheck`、`bun run lint`、`bun run test`、`bun run build`、
  CLI run/fleet smoke、dist package version、built CLI `--version`、`git diff --check`。
  `bun publish --dry-run --access public` 已完成 pack stage，共 34 files /
  3.17MB，并在本机 npm auth boundary 停止。
