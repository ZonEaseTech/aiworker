# QA-004 0.6.0 published claude-code Soul/Brain end-to-end debug campaign

- **status**: completed
- **priority**: P1
- **owner**: aiworker-maintainer
- **createdAt**: 2026-05-04 22:45
- **claimedAt**: 2026-05-04 22:45
- **completedAt**: 2026-05-04 22:45
- **plan**: TBD
- **relatesTo**: BUG-054, BUG-055, BUG-056, BUG-057, BUG-058, BUG-059, TODO-009, TODO-010, TODO-011

## Description

针对 `@zonease/aiworker-cli@0.6.0` published 版本，使用 claude-code 2.1.126 executor，做一次
worker / Soul / Brain 端到端调试。重点验证：

- npm install -g 安装路径与 CLI 命令树
- `aiworker init` 在 6 个 Soul preset 下产出物的差异化
- `aiworker doctor` / `executor doctor` / `executor select` 的诊断与切换
- brain artifact registry / scope manifest / brief compiler
- brain admission MVP CRUD + materializer（注入 fixture 直写 worker.db）
- claude-code executor readiness + 单轮对话
- worker serve + bearer-auth + REST `/api/worker/info brainSummary`
- 跨 Soul 业务采样：用 LLM 实际回答验证 SOUL.md / AGENT.md / MEMORY.md 是否真的引导推理
- brain 决策层 LLM evaluator 是否真的走 LLM

## Method

调试根：`/home/ben/projects/debug-aiworker/qa-2026-05-04/`

- 隔离 npm prefix 装 0.6.0：`/home/ben/projects/debug-aiworker/qa-2026-05-04/npm-prefix/`
- 在 `proj-dev / proj-hr-recruiting / proj-finance-ops / proj-devops-sre /
  proj-product-designer / proj-general-assistant` 6 个 project 分别 init
- fake-claude shim：`bin/claude` 把真实 claude 子进程的 argv / stdin / env dump 到
  `dump/claude-*.txt`
- 业务采样脚本：`samples/run_one.sh` 跑 `aiworker run` 并提取 final text +
  intent_decision / capability_decision / quality_gate 三个事件
- 直写 worker.db 注入 admission fixture（CLI 没有 propose 入口，详见 TODO-009）

## Findings

10 份独立 finding markdown：`/home/ben/projects/debug-aiworker/qa-2026-05-04/findings/`

按严重度排序后已按 PMA 规范登记到 task：

| Finding | Task |
|---------|------|
| BUG-1 brief --artifact undefined | [BUG-054](BUG-054.md) `P2` |
| BUG-3 admission body 落盘明文 secret | [BUG-055](BUG-055.md) `P0` 安全 |
| BUG-5 CRITICAL Project Brain 从未注入 LLM | [BUG-056](BUG-056.md) `P0` 产品定位 |
| BUG-6 brain LLM evaluator 100% JSON.parse 失败 | [BUG-057](BUG-057.md) `P1` |
| BUG-7 4 个 Soul self-intro 几乎一致 | 并入 BUG-056 业务证据段 |
| UX-3 admission 缺 propose 入口 | [TODO-009](TODO-009.md) `P3` |
| UX-4 admission list/show schema drift 整体崩溃 | [BUG-058](BUG-058.md) `P2` |
| UX-5 evidence schema strip summary | [TODO-010](TODO-010.md) `P3` |
| UX-6 unsupported kind apply 不写 audit | [BUG-059](BUG-059.md) `P3` |
| UX-7 next-steps 写死 codex | [TODO-011](TODO-011.md) `P3` |

汇总报告：`/home/ben/projects/debug-aiworker/qa-2026-05-04/REPORT.md`
prod-grade Soul/Brain 调教建议：`findings/SOUL-prod-grade-suggestions.md`

## Verified Working

- `aiworker init` 6 个 preset → scope.json / SOUL.md / capability-packs / toolsets / policy 按 preset 正确分化
- `aiworker doctor` / `executor doctor` 诊断结论合理
- `aiworker brain admission` 状态机严格（pending → approved / rejected → applied / failed）
- `aiworker brain admission apply --commit` 正确写 `.aiworker/memories/<topic>.md` + MEMORY.md 索引行
- `aiworker run` 流式 delta 不重复（BUG-052 / BUG-053 fix 已生效）
- `aiworker serve` bearer-auth fail-closed / `/api/worker/info brainSummary` 字段完整 / admission default redact
- claude-code executor readiness 探测 + 子进程拉起正常

## Out of Scope

- 启 gateway 验证 fleet 控制面是否真的不复制 brain payload（PLAN-103 文档承诺；REST 层
  brainSummary 是 aggregate 不含 payload，方向正确；启 gateway 流程留给后续）
- codex / acp / cursor / mcp 其它 executor adapter 是否同样 BUG-056（架构上推断同样）
- channel inbound 验签
- evolution cron 写 ROLLUP.md 真实路径

## Notes

本次调试是黑盒安装 + 必要时直写 worker.db 注入 fixture，不修改 published 包；所有 finding
都附 fake-claude shim 抓到的 stdin / argv 证据 + `samples/` 下完整 LLM 回答原文，便于复现。
