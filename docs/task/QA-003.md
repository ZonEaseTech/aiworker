# QA-003 Record Soul brain executor validation campaign

- **status**: completed
- **priority**: P1
- **owner**: local
- **createdAt**: 2026-05-03 23:33
- **claimedAt**: 2026-05-03 23:33
- **completedAt**: 2026-05-03 23:33
- **plan**: PLAN-080

## 描述

记录 2026-05-03 在 `/Users/ben/projects/aiben` 执行的本地 Soul / brain /
executor 初始化验证样本，并把发现的优化和修复项转成 PMA 后续任务。

本次验证保持真实用户 `HOME`，让 Codex CLI 读取本机登录态；只清理和重建
`/Users/ben/projects/aiben/.aiworker` 下的 AIWorker project state。未连接
fleet/gateway，未把 secret 或 bearer token 写入仓库文档。

## 覆盖范围

1. 9 个内置 Soul fresh `init`、`doctor`、`brain status`、`brain skills`、
   `brain memories` 和 `executor doctor` 静态样本。
2. 9 个内置 Soul 的真实 Codex-backed `aiworker run` identity JSON 样本。
3. Codex executor selection、executor-native MCP 声明、doctor 和 dry-run
   projection。
4. Claude Code executor-native MCP 声明、doctor 和 dry-run projection。
5. Codex executor 实际文件创建/读取手脚探针。

## 结论

- 9 个内置 Soul 都能在真实回复中明确自己的身份、主要职责、思考取舍和风险边界。
- 9 个 Soul 的 runtime brain 都挂载 project-scope writable
  `local-filesystem`，且能发现非敏感 probe skill / memory。
- 选择 `codex/default` 并声明 executor-native MCP 后，Codex executor doctor
  为 `PASS`；额外声明 Claude Code MCP 后，Claude Code executor doctor 也为
  `PASS`。
- Codex executor 实际创建并读回了 `tmp/aiworker_executor_probe_*.txt` marker
  文件，说明真实手脚能力可用。

## Follow-Up Tasks

- `BUG-050`: current Codex app-server live tool activity does not surface as
  AIWorker `orchestrator.tool_call` events.
- `BUG-051`: `aiworker executor mcp add --arg -y` is parsed as an unknown CLI
  option unless written as `--arg=-y`.
- `TODO-008`: turn this one-off manual matrix into a repeatable local validation
  harness.

## ActiveForm

Completed local Soul / brain / executor validation and recorded follow-up PMA
tasks.

## Dependencies

- **blocked by**: none
- **blocks**: BUG-050, BUG-051, TODO-008
- **relates to**: FEAT-046, FEAT-047, QA-002, PLAN-073, PLAN-074, PLAN-075,
  PLAN-077, PLAN-078

## Notes

- Evidence directory: `/Users/ben/projects/aiben/tmp/brain-executor-debug-20260503-222707`.
- Identity matrix: `/Users/ben/projects/aiben/tmp/codex-soul-identity-matrix-20260503-222944`.
- Executor hand probe: `/Users/ben/projects/aiben/tmp/codex-tool-hand-probe-20260503-223228`.
- Dual-engine executor capability probe:
  `/Users/ben/projects/aiben/tmp/executor-capability-dual-engine-20260503-223441`.
- Focused regression suite passed after the validation:
  `bun test apps/cli/src/commands/worker/init.integration.test.ts apps/cli/src/commands/worker/executor.test.ts apps/cli/src/commands/worker/run.test.ts packages/core/src/worker/orchestrator/service.history.test.ts packages/core/src/worker/executor/engines/codex/executor.test.ts`.
