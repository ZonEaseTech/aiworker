# REL-026 发布 aiworker CLI 0.10.2

- **status**: in_progress
- **priority**: P1
- **owner**: local
- **createdAt**: 2026-05-07 17:49
- **claimedAt**: 2026-05-07 17:49
- **plan**: PLAN-162
- **sourceObjective**: 将 Worker Admin Chat 与 Config 的两个 Web patch
  发布到 npm / GitHub Release。
- **relatesTo**: BUG-088, BUG-089, REL-025, PLAN-161

## 背景

`@zonease/aiworker-cli@0.10.1` 已完成发布包 full matrix 验证。随后本地
`main` 累积两个未推送 patch commit：

- `c8cb3f8 fix(web): 修复 Worker Chat 最后一条重复渲染`
- `fd04875 fix(web): 修复 Worker Config 保存条布局`

发布前状态：

- 本地 `main` 相比 `origin/main` ahead 2。
- npm latest 为 `0.10.1`。
- `apps/cli/package.json` version 为 `0.10.1`。
- 最新 tag 为 `v0.10.1`。

## 范围

- 将 `apps/cli/package.json` 与 README latest 更新到 `0.10.2`。
- 运行本地 release gate，重点覆盖 Web test/typecheck/lint/build 与整体发布产物。
- 构建并检查 CLI dist 产物版本。
- 执行 publish dry-run 到本机 npm auth boundary。
- 提交 release bump。
- 推送 `main` 与 annotated tag `v0.10.2`。
- 监控 tag-triggered GitHub Actions release workflow。
- 验证 npm latest、显式 package smoke 与 GitHub Release assets。

## 非范围

- 不修改运行时代码；本次只发布已存在 Web patch commit。
- 不重新跑 full Governance Kernel matrix；本次变更不触碰 Brain /
  executor runtime。
- 不处理 fleet / gateway。

## 验收标准

1. 本地 release gate 全部通过。
2. `main` 与 `v0.10.2` 推送成功。
3. GitHub Actions release workflow 成功。
4. npm latest 解析到 `0.10.2`。
5. `bunx @zonease/aiworker-cli@0.10.2 --version` 报告
   `aiworker/0.10.2`。
6. GitHub Release `v0.10.2` 存在且含四个平台 binary assets。

## 记录

- 2026-05-07 17:49：任务打开。确认本地 `main` ahead 2，npm latest 仍为
  `0.10.1`，发布目标为 `0.10.2` patch。
- 2026-05-07 23:13：本地 release gate 已通过到 build、CLI smoke、dist
  version、`git diff --check` 与 publish dry-run pack stage。publish dry-run
  打包 32 files / 3.1 MB 后在本机 npm auth boundary 停止，符合预期。

## 验证

- 2026-05-07 17:50：`bun install --frozen-lockfile` passed。
- 2026-05-07 17:50：`bun run typecheck` passed。
- 2026-05-07 17:50：`bun run lint` passed。
- 2026-05-07 17:51：`bun run test` passed。
- 2026-05-07 23:13：`bun run build` passed。
- 2026-05-07 23:13：
  `bun run --filter '@zonease/aiworker-cli' smoke:aiworker-run` passed。
- 2026-05-07 23:13：
  `bun run --filter '@zonease/aiworker-cli' smoke:aiworker-fleet` passed。
- 2026-05-07 23:13：`apps/cli/dist/package.json` version is `0.10.2`。
- 2026-05-07 23:13：`bun apps/cli/dist/aiworker-bun.js --version` reports
  `aiworker/0.10.2 darwin-arm64 node-v24.3.0`。
- 2026-05-07 23:13：`git diff --check` passed。
- 2026-05-07 23:13：
  `cd apps/cli/dist && bun publish --dry-run --access public` packed 32 files
  / 3.1 MB, then stopped at the expected local npm authentication boundary。
