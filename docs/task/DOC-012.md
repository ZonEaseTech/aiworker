# DOC-012 Clean active documentation map

- **status**: completed
- **priority**: P0
- **owner**: codex
- **createdAt**: 2026-05-13 20:58
- **plan**: PLAN-303
- **relatesTo**: AGENTS.md, README.md, docs/cli.md, docs/deployment.md, docs/executor-engines.md, docs/e2e-smoke.md, docs/governance-node-status.md

## 背景

`AGENTS.md` 与 `docs/architecture.md` 已经收敛为当前主入口，但根 `docs/` 下仍有多份不同阶段
留下的文档。部分页面没有活跃引用，部分页面描述旧命令树或旧治理路径，会让后续 agent 重新
读到被废弃的产品心智。

## 目标

1. 删除断链或纯历史的 root-level docs。
2. 将 CLI、deployment、executor engine 文档改成当前 Host / Soul App 合同下的按需参考。
3. 让 `AGENTS.md` 的指引更清爽：必读入口只保留架构合同，其他文档按任务读取。
4. 保持 README 中有清晰 docs map，避免保留的文档继续变成孤岛。

## 非目标

- 不改运行时代码、CLI 命令实现、API、Web 或测试。
- 不重写历史 PMA/changelog 记录。
- 不新增替代 smoke 脚本。

## 验收标准

- `docs/e2e-smoke.md` 与 `docs/governance-node-status.md` 已删除。
- 活跃文档不再引用被删除的两个页面。
- `docs/cli.md` 不再描述 `brief/run` 旧命令树。
- `docs/deployment.md` 与 `docs/executor-engines.md` 对齐 Host / Soul App 本地 daemon 合同。
- `git diff --check` 通过。

## 完成记录

- 删除 `docs/e2e-smoke.md`，因为它引用的 PLAN-004 smoke 脚本已不存在。
- 删除 `docs/governance-node-status.md`，避免旧治理过渡说明继续成为第三个开发入口。
- 重写 `docs/cli.md`，对齐当前 `app/worker/workspace/session/turn` CLI 命令树。
- 刷新 `docs/deployment.md` 与 `docs/executor-engines.md`，对齐 Host / Soul App local daemon 合同。
- 更新 `AGENTS.md` 与 README 文档地图，让活跃文档按任务读取。

## 验证

- `test ! -e docs/e2e-smoke.md && test ! -e docs/governance-node-status.md`
- `rg -n "docs/e2e-smoke|e2e-smoke\\.md|docs/governance-node-status|governance-node-status\\.md" AGENTS.md README.md docs/architecture.md docs/cli.md docs/deployment.md docs/executor-engines.md docs/soul-app-developer.md`
- `rg -n "brief create|run start|run list|run show|run cancel|executor select|executor doctor" docs/cli.md`
- `rg -n "durable org memory|governance-first|Project Brain node|Fleet summary" AGENTS.md README.md docs/cli.md docs/deployment.md docs/executor-engines.md`
- `git diff --check`
- code-review-graph skipped because this change only touches documentation, root agent instructions and skill planning markdown.
