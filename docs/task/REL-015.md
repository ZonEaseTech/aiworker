# REL-015 发布 aiworker CLI 0.8.0

- **status**: completed
- **priority**: P1
- **owner**: aiworker-maintainer
- **createdAt**: 2026-05-05 06:30
- **claimedAt**: 2026-05-05 06:30
- **completedAt**: 2026-05-05 06:50
- **plan**: PLAN-113

## 描述

发布 `@zonease/aiworker-cli@0.8.0`，作为 `0.7.0` 之后的 minor release，交付 QA-005（0.7.0 published claude-code Soul/Brain 端到端调试）登记的 11 个 task 通过 PLAN-109 / PLAN-110 / PLAN-111 / PLAN-112 四个 plan 的修复，包括 P0 admission redact 安全 / P1 brain memory recall / P1 dev-Soul tool loop / P2 OpenAPI / P2 高风险 intent / P2 LLM evaluator latency / P3 doctor UX 等关键 + UX 缺陷。

本次发版包含：

- `PLAN-109` Brain brief / admission read-path 收口。覆盖 `BUG-060` (P1 brain memory body 注入) / `BUG-061` (P1 admission redact 双闸) / `BUG-062` (P3 brief artifact-summary 防御) / `TODO-012` (P2 secret-scan ruleset 扩展)。
- `PLAN-110` Decision pipeline 强化。覆盖 `BUG-063` (P1 dev-Soul tool loop + dead-loop detector) / `BUG-064` (P2 intent risk 词典扩展) / `TODO-013` (P2 LLM evaluator 并行 + budgetMs)。
- `PLAN-111` Worker API surface 修复。覆盖 `BUG-065` (P2 OpenAPI paths) / `TODO-014` (P3 engine debug env allowlist) / `TODO-016` (P2 serve port preflight + pid-file + /health 自描述)。
- `PLAN-112` Doctor first-run UX。覆盖 `TODO-015` (P3 doctor summary line + fresh-init 抑制 + 命名消歧)。

## ActiveForm

发布 aiworker CLI 0.8.0

## Dependencies

- **blocked by**: none
- **blocks**: published CLI 用户拿到 PLAN-109..112 的 brain memory recall / admission read-path 安全 / dev-Soul tool loop / OpenAPI / serve preflight / doctor UX 修复
- **relates to**: PLAN-109, PLAN-110, PLAN-111, PLAN-112, PLAN-113, QA-005, BUG-060, BUG-061, BUG-062, BUG-063, BUG-064, BUG-065, TODO-012, TODO-013, TODO-014, TODO-015, TODO-016

## Notes

- 2026-05-05 06:30：npm latest、GitHub Release 和远端 tag 当前均为 `0.7.0`；本次按 `0.8.0` minor release 执行：BUG-061 改了 admission read-path 默认行为（之前 `redacted: true` 但实际 plaintext，现在双闸真实 redact）+ 新增 `OrchestratorDeadLoopConfig` / `OrchestratorQualityGateConfig.budgetMs` worker config schema 字段 + 新增 `--pid-file` CLI flag + safe-env 显式允许 `AIWORKER_DEBUG_*` / `DEBUG_*` prefix + Soul module schema 加 `vagueContextStrategy` 字段（9 个内置 Soul 全部填充），用户可观察行为本质改变。
- 2026-05-05 06:50：release commit `2230deb chore(release): 发布 CLI 0.8.0` + annotated tag `v0.8.0` 已 push 到 origin；GitHub Actions release workflow run id `25377089930` (job `74415001398`) 2m5s 全绿；npm `@zonease/aiworker-cli` `latest=0.8.0` 已上线；GitHub Release `v0.8.0` 已发布（非 draft / 非 prerelease），4 个平台 binary 全部 uploaded（darwin-arm64 23.95 MB / darwin-x64 26.37 MB / linux-arm64 39.65 MB / linux-x64 39.98 MB）。

## Validation

- `bun run typecheck`
- `bun run lint`
- `bun run test`
- `bun run build`
- `git diff --check`
- `apps/cli/dist/package.json` 版本字段 = `0.8.0`
- GitHub Actions release workflow 全绿
- `npm view @zonease/aiworker-cli version` → `0.8.0`
- `gh release view v0.8.0` → 4 个平台 binary uploaded
