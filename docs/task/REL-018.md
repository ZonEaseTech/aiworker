# REL-018 发布 aiworker CLI 0.9.2

- **status**: completed
- **priority**: P1
- **owner**: local
- **createdAt**: 2026-05-06 09:50
- **claimedAt**: 2026-05-06 09:50
- **completedAt**: 2026-05-06 09:54
- **plan**: PLAN-132

## 描述

发布 `@zonease/aiworker-cli@0.9.2`，作为 `0.9.1` 之后的 patch release，标记
Brain Governance Kernel 回归 harness 在源码与已发布 CLI 双侧的 full 5×2
matrix 证据完成节点。

本次发版包含：

- `PLAN-128 / QA-011`：admission 正向 roundtrip 证据（`pending → approved →
  applied` 写 canonical memory + index entry + brain brief projection），
  source-local 与 cli-release-local 0.9.1 双模式 compact 通过。
- `PLAN-129 / QA-012`：admission 负向路径与 secret-scan-block 证据（reject
  和 BUG-055 secret-scan block），source-local 与 cli-release-local 0.9.1 双
  模式 compact 通过。
- `PLAN-130 / QA-013`：source-local full 5×2 matrix（5 Soul × 2 executor）
  10 对 × 30 检查 = 300 PASS / 0 FAIL / 0 SKIPPED。
- `PLAN-131 / QA-014`：cli-release-local 0.9.1 full 5×2 matrix
  300 PASS / 0 FAIL / 0 SKIPPED。
- `docs/governance-node-status.md`：Project Brain governance node 最终评估
  报告，含 conformance 表与残留边界。

本次 release **不修改产品代码**：`apps/`、`packages/`、`drizzle/` 都未变更，
变化集中在 `scripts/governance-kernel-harness.ts` 和 `docs/*`。这是一个
"regression-coverage milestone" 类型的 patch release，目的是把 dev-only
的回归基础设施和最终评估报告作为 release artifact 标记。

## ActiveForm

发布 aiworker CLI 0.9.2

## Dependencies

- **blocked by**: none
- **blocks**: published CLI 用户可在 npm 拿到与 0.9.1 行为一致的最新 patch；
  README / changelog 指向新增的 governance-node-status 评估报告
- **relates to**: PLAN-128, PLAN-129, PLAN-130, PLAN-131, QA-011, QA-012,
  QA-013, QA-014

## Notes

- 2026-05-06 09:50：`v0.9.1` 之后 12 个 commit，全部为 `test:` / `docs:` /
  `docs(pma):` 前缀，未触及 `apps/cli/src/`、`packages/`、`drizzle/`。
- 2026-05-06 09:51：本地 release gates 全通过（`bun install --frozen-lockfile`、
  `bun run typecheck`、`bun run lint`、`bun run test` 175 PASS / 0 FAIL、
  `bun run build`、CLI run / fleet smoke、dist manifest version=`0.9.2`、
  built CLI `--version` 返回 `aiworker/0.9.2 linux-x64 node-v24.3.0`、
  `git diff --check`）。
- 2026-05-06 09:54：发布完成。
  - Release commit `4b5697c chore(release): 发布 CLI 0.9.2` + annotated tag
    `v0.9.2` 已 push 到 origin。
  - GitHub Actions release workflow run id `25428320371` 全绿。
  - npm `@zonease/aiworker-cli` `latest=0.9.2` 已上线；
    `bunx @zonease/aiworker-cli@0.9.2 --version` 返回
    `aiworker/0.9.2 linux-x64 node-v24.3.0`。
  - GitHub Release `v0.9.2` 非 draft / 非 prerelease；4 个平台 binary 全部
    uploaded（darwin-arm64 23.96 MB / darwin-x64 26.38 MB / linux-arm64
    39.66 MB / linux-x64 39.99 MB）。

## Validation

- `bun install --frozen-lockfile`
- `bun run typecheck`
- `bun run lint`
- `bun run test`
- `bun run build`
- `bun run --filter '@zonease/aiworker-cli' smoke:aiworker-run`
- `bun run --filter '@zonease/aiworker-cli' smoke:aiworker-fleet`
- `apps/cli/dist/package.json` 版本字段 = `0.9.2`
- `bun apps/cli/dist/aiworker-bun.js --version` → `aiworker/0.9.2 ...`
- `git diff --check`
- publish dry-run pack 阶段
- GitHub Actions release workflow 全绿
- `npm view @zonease/aiworker-cli version` → `0.9.2`
- `bunx @zonease/aiworker-cli@0.9.2 --version` → `aiworker/0.9.2 ...`
- `gh release view v0.9.2` → 4 个平台 binary uploaded
