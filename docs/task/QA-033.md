# QA-033 Worker Web interaction polish regression validation

- **status**: completed
- **priority**: P0
- **owner**: local
- **createdAt**: 2026-05-11 10:38
- **plan**: PLAN-243
- **relatesTo**: REFACTOR-063, apps/web

## Background

The follow-up repair is driven by manual UI findings and must be checked in a
real browser. The validation should cover both listed defects and additional
layout/accessibility problems found through Playwright MCP.

## Goal

Validate that the revised Worker Web interaction model is stable across
home, empty-worker, workspace, session, dialog, select, and mobile states.

## Acceptance Criteria

- Playwright MCP covers create worker dialog, select menu, worker list, no
  worker Soul list, workspace route, session route, session return actions,
  and narrow mobile viewport.
- Browser inspection reports no horizontal overflow on desktop or 390px mobile.
- Keyboard and accessible names remain present for custom select and dialogs.
- Focused automated tests cover the highest-risk behavior changes.
- code-review-graph result is recorded before completion.

## Verification

- `bun run --filter '@zonease/aiworker-web' typecheck`
- `bun run --filter '@zonease/aiworker-web' lint`
- `bun run --filter '@zonease/aiworker-web' test`
- `bun run --filter '@zonease/aiworker-web' build`
- `git diff --check`
- `bun run crg:update`
- `bun run crg:review`

## Closeout

Completed on 2026-05-11.

- Automated validation passed for Worker Web typecheck, lint, RTL tests, and
  production build including the studio CSS quality check.
- Playwright MCP validated `http://127.0.0.1:9217/` across desktop and 390px
  mobile viewports. It confirmed no native `select` elements, no horizontal
  overflow, integrated custom select menus, centered workspace content, direct
  session return-to-workspace actions, and the repaired mobile workspace scroll
  layout.
- The live 9217 database already contains a worker, so the no-worker Soul list
  state was validated through focused RTL coverage instead of deleting local
  operator data.
- code-review-graph review completed with risk score `0.60`, 0 affected flows,
  and remaining test gaps limited to UI component entities covered by focused
  RTL and Playwright evidence.
