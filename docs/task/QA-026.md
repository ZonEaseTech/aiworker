# QA-026 Executor non-interference release validation

- **status**: in_progress
- **priority**: P1
- **owner**: local
- **createdAt**: 2026-05-09 16:50
- **claimedAt**: 2026-05-09 16:50
- **release**: REL-032
- **plan**: PLAN-193
- **relatesTo**: REFACTOR-026

## Objective

Validate that `@zonease/aiworker-cli@0.12.2` is safe to publish as the
executor non-interference patch release.

## Source Validation

- Real worker target: `/Users/ben/projects/my-aiworker`, port `9217`.
- Source commit under test before version bump: `229aa95`.
- Source worker result:
  - `/api/worker/info` showed `runtimeVersion=0.12.1`,
    `executor=codex/default`, no `controlExecutor`, and disabled conversation
    classifier.
  - Successful Codex chat produced a persisted user/assistant transcript and
    a Case with 0 tool events.
  - Controlled HTTP-stub executor failure produced a persisted assistant
    failure message and a `needs_rerun` Case.
  - Slow native Codex adapter stub completed after 125079 ms without a 120s
    kill timer.

## Release Validation

- Passed: source gates after version bump.
  - `bun run typecheck`
  - `bun run lint`
  - `bun run test`
  - `bun run build`
  - `git diff --check`
- Passed: dist version and publish dry-run.
  - `bun apps/cli/dist/aiworker-bun.js --version` ->
    `aiworker/0.12.2 darwin-arm64 node-v24.3.0`.
  - `bun pm pkg get version --cwd apps/cli/dist` -> `0.12.2`.
  - `cd apps/cli/dist && bun publish --dry-run --access public` packed
    34 files / 3.21MB, then stopped only at expected local npm auth boundary.
- Pending: tag/release workflow.
- Pending: npm/GitHub Release verification.
- Pending: published-package compact governance harness.

## Decision

Pending final release validation.
