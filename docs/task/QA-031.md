# QA-031 Worker-first workflow validation

- **status**: completed
- **priority**: P0
- **owner**: local
- **createdAt**: 2026-05-11 01:11
- **claimedAt**: 2026-05-11 01:30
- **plan**: PLAN-237
- **relatesTo**: apps/api, apps/web, apps/cli, packages/storage-sqlite, packages/core

## Background

The worker-first refactor changes storage, API, CLI, and Web entry flow. It
needs a focused validation pass across programmatic tests and browser behavior.

## Goal

Prove the new top-level worker flow works end to end and does not regress the
existing session/chat/artifact loop.

## Acceptance Criteria

- Focused storage/core/API/CLI/Web tests pass.
- Web browser validation covers worker creation, worker route, workspace
  creation, session creation, and continuing a session.
- Mobile viewport remains free of horizontal overflow.
- code-review-graph review is recorded because code files change.

## Result

- Focused storage, core, API, CLI, and Web gates passed.
- Root `typecheck`, `lint`, `test`, and `build` passed.
- Follow-up 2026-05-11 02:02: Playwright MCP reproduced that clicking
  `创建 worker` on the real `http://127.0.0.1:9217/` daemon submitted
  `POST /api/local/workers` but failed with HTTP 500 because the upgraded local
  `worker.db` still had legacy unique index `workers_soul_idx`.
- Worker migrations now repair legacy unique `workers_soul_idx` into a
  non-unique index, and storage tests cover creating two workers bound to the
  same Soul after migration repair.
- Follow-up Playwright MCP validation on `http://127.0.0.1:9217/` clicked
  through the actual flow:
  - created worker `hr-playwright-worker-1740-31ce9d16`;
  - created workspace `3f240c55-a457-4c5b-be43-f0d5dd66628d`;
  - created session `f3e44590-08ec-4093-a066-e8bff979f443`;
  - reached
    `/workers/hr-playwright-worker-1740-31ce9d16/workspaces/3f240c55-a457-4c5b-be43-f0d5dd66628d/sessions/f3e44590-08ec-4093-a066-e8bff979f443`;
  - observed engine status `成功`, 1 artifact, and pending review state.
- Browser validation against `http://127.0.0.1:9328/` with
  `AIWORKER_HOME=/tmp/aiworker-worker-first-validation` created:
  - worker `hr-hr-browser-worker-c5d58049`;
  - workspace `cabfc327-68a5-4bc1-b815-9b5895722e5d`;
  - session `380c7b4c-ae8d-4184-9406-0570479a6f07`;
  - two succeeded turns, two artifact records, and a review-required artifact.
- The canonical route reached
  `/workers/hr-hr-browser-worker-c5d58049/workspaces/cabfc327-68a5-4bc1-b815-9b5895722e5d/sessions/380c7b4c-ae8d-4184-9406-0570479a6f07`.
- Mobile check at `390x844` reported `docScrollWidth=390`,
  `bodyScrollWidth=390`, and `hasHorizontalOverflow=false`.

## Verification

- `bun run --filter '@zonease/aiworker-storage-sqlite' test`
- `bun run --filter '@zonease/aiworker-storage-sqlite' typecheck`
- `bun run --filter '@zonease/aiworker-core' test`
- `bun run --filter '@zonease/aiworker-api' typecheck`
- `bun run --filter '@zonease/aiworker-api' test`
- `bun run --filter '@zonease/aiworker-cli' typecheck`
- `bun run --filter '@zonease/aiworker-cli' test`
- `bun run --filter '@zonease/aiworker-web' typecheck`
- `bun run --filter '@zonease/aiworker-web' lint`
- `bun run --filter '@zonease/aiworker-web' test`
- `bun run --filter '@zonease/aiworker-web' build`
- `bun run typecheck`
- `bun run lint`
- `bun run test`
- `bun run build`
- `git diff --check`
- `bun run crg:update`
- `bun run crg:review`

## code-review-graph

- `detect-changes`: 19 changed files, 83 changed functions/classes, 0 affected
  flows, 61 reported test gaps, risk score `0.60`.
- Reported untested areas: `createHrWorker`, `bootstrapWorkerApp`, `template`,
  `workspace`, `artifact`.
- `get_affected_flows`: 0 affected flows.
