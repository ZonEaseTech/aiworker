# QA-002 Run local Codex-backed worker validation campaign

- **status**: completed
- **priority**: P1
- **owner**: unassigned
- **createdAt**: 2026-05-02 20:39
- **discoveredAt**: 2026-05-02 20:39

## Description

Run a local real-machine validation pass for a project-scoped worker in
`/Users/ben/projects/aiben`, using the real local Codex executor environment
and Worker Admin Web UI. Do not run fleet or gateway validation in this pass.

## Coverage

1. Project-scope `worker init`, `worker doctor`, and `worker executor doctor`.
2. CLI `worker run` continuity with a stable `--chat-id`.
3. Worker HTTP API auth boundaries and static Worker Admin serving.
4. Worker Admin Chat, Config, Secrets, Test, Cron, Approvals, and mobile
   viewport smoke coverage through Playwright.
5. Worker database spot checks for task, conversation, and cron persistence.

## Confirmed Results

- CLI `worker run --chat-id real-codex-session-1` preserved Codex-backed
  continuity across turns.
- Authenticated Worker Admin pages loaded and core Secrets/Cron CRUD flows
  worked; the fake cron entry was deleted after validation.
- Worker Admin mobile layout no longer reproduced the prior fixed-sidebar
  overflow class of issue.
- Follow-up bugs and polish tasks were recorded instead of being fixed in this
  validation session.

## Follow-Up Tasks

- `BUG-043`: Worker Admin Chat event stream times out before slow replies.
- `BUG-044`: Worker Admin Chat creates a new conversation instead of
  continuing the selected one.
- `BUG-045`: Orchestrator task rows remain `queued` after successful execution.
- `BUG-046`: Executor tiny probe can hang beyond its configured timeout.
- `BUG-047`: Worker Admin no-token state renders raw API auth errors.
- `TODO-007`: Worker Admin validation UX polish items from this pass.

## ActiveForm

Completed local worker validation and recorded follow-up tasks.

## Dependencies

- **blocked by**: none
- **blocks**: BUG-043, BUG-044, BUG-045, BUG-046, BUG-047, TODO-007
- **relates to**: FEAT-035, FEAT-037, FEAT-045, REL-009

## Notes

- 2026-05-02 20:39 Used only local worker state under
  `/Users/ben/projects/aiben/.aiworker`; fleet and gateway validation were
  intentionally skipped for this pass.
- 2026-05-02 20:39 The worker server was started from the built CLI bundle on
  `127.0.0.1:9327`, with the real user `HOME` preserved for Codex executor
  authentication.
- 2026-05-02 20:39 No source fix was implemented in this session.
