# REL-009 发布 aiworker CLI 0.5.0

- **status**: completed
- **priority**: P1
- **owner**: local
- **createdAt**: 2026-05-02 19:41
- **claimedAt**: 2026-05-02 19:41
- **completedAt**: 2026-05-02 19:51
- **plan**: PLAN-064

## 描述

发布 `@zonease/aiworker-cli@0.5.0`，作为 `0.4.11` 之后的 minor release，并把测试服 gateway/fleet 升级到该版本。

本次发版包含 `0.4.11` 之后已合入 main 的改动：

- REFACTOR-015 / PLAN-062：CLI root shortcut 收敛为本地 worker，远端控制面迁到 `fleet`，gateway 生命周期迁到 `gateway`。
- FEAT-045 / PLAN-063：新增 `aiworker up` / `aiworker worker up` 本地 worker 快速启动路径。

Release checklist：

1. 把 CLI package 版本从 `0.4.11` bump 到 `0.5.0`。
2. 跑完本地 release quality gates。
3. 构建 publish artifact，并确认 `apps/cli/dist/package.json` 报告 `0.5.0`。
4. 通过现有 tag-triggered GitHub release workflow 发布。
5. 验证 npm `latest` 解析到 `0.5.0`，并 smoke 已发布 CLI。
6. 使用 `aissh` 升级测试服 gateway/fleet 到 `0.5.0`，验证服务状态、gateway `/health` 与 CLI version。

## ActiveForm

发布 aiworker CLI 0.5.0 并升级测试服 gateway/fleet

## Dependencies

- **blocked by**: none
- **blocks**: 测试服验证 `aiworker up` 与新 CLI IA
- **relates to**: REFACTOR-015, FEAT-045, PLAN-062, PLAN-063, PLAN-064

## Notes

- 2026-05-02 19:41：用户确认按建议发版；本次选择 `0.5.0`，因为 CLI IA 是 pre-1.0 破坏性收敛，minor 版本能给安装用户更清晰的信号。
- 2026-05-02 19:44：本地 release gates 通过，`apps/cli/dist/package.json` 报告 `0.5.0`；`bun publish --dry-run --access public` 完成 26 files / 2.45MB pack，停在本机 npm authentication boundary。
- 2026-05-02 19:44：修复两个 macOS 本地 gate 的路径断言噪声：测试临时目录统一转 canonical realpath，避免 `/var` 与 `/private/var` 字符串比较导致 root test 失败。
- 2026-05-02 19:48：`v0.5.0` GitHub Actions release workflow `25251183256` 成功；npm latest 解析到 `0.5.0`；GitHub Release 含 linux-x64、linux-arm64、darwin-x64、darwin-arm64 tarball。
- 2026-05-02 19:49：published CLI smoke 通过：显式 bin invocation 报告 `aiworker/0.5.0`，`up --help` 正常，`up --soul developer --dry-run --no-open --no-serve-web` 正常完成。发现 dry-run 未传 `--port` 时显示 `port : NaN`，记录为 BUG-042，非本次 release blocker。
- 2026-05-02 19:50：测试服 gateway/fleet 已升级到 `0.5.0`：gateway service active，`/health` ok，`/admin/`、CSS、JS 静态资源 200，`aiworker fleet list` 成功返回。
