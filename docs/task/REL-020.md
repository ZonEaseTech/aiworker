# REL-020 发布 aiworker CLI 0.9.4

- **status**: in_progress
- **priority**: P1
- **owner**: local
- **createdAt**: 2026-05-06 14:07
- **claimedAt**: 2026-05-06 14:07
- **plan**: PLAN-139

## 描述

发布 `@zonease/aiworker-cli@0.9.4`，作为 `0.9.3` 之后的 patch release，
交付远端 fleet-hosted Worker Admin 与 OTP worker reconnect 修复。

本次发版包含：

- `BUG-079 / PLAN-136`：恢复 public Caddy `/w*` route，fleet 域名可以托管
  Worker Admin shell 与 gateway worker bridge。
- `BUG-080 / PLAN-137`：已批准 OTP worker 可以继续在 `/enroll-ws` 上用
  registered worker token 重连，不需要 operator Basic Auth。
- `BUG-081 / PLAN-138`：`/w/:workerId/api/worker/*` 改由 gateway 校验 worker
  bearer token；Caddy 不再对 `/w*` 做 Basic Auth，避免覆盖 Worker Admin 的
  `Authorization: Bearer <worker token>`。

## ActiveForm

发布 aiworker CLI 0.9.4

## Dependencies

- **blocked by**: none
- **blocks**: npm latest 与 GitHub Release 提供远端 fleet Worker Admin 修复
- **relates to**: BUG-079, BUG-080, BUG-081, PLAN-136, PLAN-137, PLAN-138

## Notes

- 2026-05-06 14:07：发版开始。当前 npm latest 为 `0.9.3`；本地 `main` 在
  `f434851`，存在本次 hotfix 工作树改动。
- 2026-05-06 14:12：本地 release gate 已通过；`bun publish --dry-run`
  完成 pack 阶段，最后因本机未登录 npm 停在 expected auth error，实际 publish
  交给 GitHub Actions `NPM_TOKEN`。

## Validation

- `bun install --frozen-lockfile`
- `bun run typecheck`
- `bun run lint`
- `bun run test`
- `bun run build`
- `bun run --filter '@zonease/aiworker-cli' smoke:aiworker-run`
- `bun run --filter '@zonease/aiworker-cli' smoke:aiworker-fleet`
- `apps/cli/dist/package.json` 版本字段 = `0.9.4`
- `bun apps/cli/dist/aiworker-bun.js --version` → `aiworker/0.9.4 ...`
- `git diff --check`
- publish dry-run pack 阶段
- GitHub Actions release workflow 全绿
- `npm view @zonease/aiworker-cli version` → `0.9.4`
- `bunx @zonease/aiworker-cli@0.9.4 --version` → `aiworker/0.9.4 ...`
- `gh release view v0.9.4` → 4 个平台 binary uploaded
