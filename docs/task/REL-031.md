# REL-031 发布 aiworker CLI 0.12.1

- **status**: in_progress
- **priority**: P1
- **owner**: local
- **createdAt**: 2026-05-09 15:08
- **claimedAt**: 2026-05-09 15:08
- **plan**: PLAN-191
- **relatesTo**: FEAT-058, QA-024, QA-025

## Context

`0.12.1` is a patch release for the Case-driven Project Brain loop after
FEAT-058. It carries correctness and production-readiness fixes after `0.12.0`:

- Case and Journal evidence is task-scoped instead of conversation-scoped.
- Pure heuristic observe-only pass no longer becomes `ready_to_ship`.
- Brain Engine reviewed pass can be used as release evidence, but high-risk
  ambient authority warning remains visible.
- Codex current native thread resume no longer replays old conversation turns
  into the executor context.

This is not a 1.0 GA release. The goal is to make the current pre-1.0 release
safe to ship as the validated Case-driven Brain loop baseline.

## Release Criteria

1. Source gates pass: `bun run check`, `bun run test`, `bun run build`.
2. `git diff --check` passes.
3. CLI source and dist manifest report `0.12.1`.
4. `cd apps/cli/dist && bun publish --dry-run --access public` reaches pack
   success.
5. A real source worker dogfood validates task-scoped Case, truthful review
   decision, lessons proposal, and native-resume prompt behavior where feasible.
6. GitHub Actions release workflow publishes npm and GitHub Release assets.
7. Published-package smoke confirms `@zonease/aiworker-cli@0.12.1`.
8. Published-package compact governance harness passes.

## Evidence

- 2026-05-09 15:52：`bun run check` passed after the release bug fix
  (`typecheck` + `eslint .`).
- 2026-05-09 15:52：`bun run test` passed across all workspace packages.
- 2026-05-09 15:52：`bun run build` passed for API, fleet/worker web bundles,
  CSS utility checks, and CLI bundle. Vite only emitted the existing chunk-size
  warnings.
- 2026-05-09 15:53：`git diff --check` passed.
- 2026-05-09 15:53：`bun apps/cli/dist/aiworker-bun.js --version` returned
  `aiworker/0.12.1 darwin-arm64 node-v24.3.0`; `bun pm pkg get version --cwd
  apps/cli/dist` returned `0.12.1`.
- 2026-05-09 15:53：`cd apps/cli/dist && bun publish --dry-run --access public`
  packed 34 files / 3.20MB and stopped at the expected local npm authentication
  boundary (`missing authentication`).
- 2026-05-09 15:54：dist CLI dogfood passed in a real temporary project-scope
  worker at `/private/tmp/aiworker-release-0.12.1-dogfood-BO6vss/project`:
  `case list` produced `needs_review` for heuristic-only output,
  `ready_to_ship` for Brain-reviewed output, and `needs_review` for production
  database risk; `case show` selected the second task's own assistant message;
  `lessons propose` created pending proposal
  `inbox-task-release-reviewed-1-43890fb3b497`.
- Dogfood caught and fixed a release blocker before tagging: default Case
  redaction over-matched `authorityMode` as an auth secret. The fix keeps
  governance/authorship fields visible while continuing to redact real auth
  material.

## Residual Risk

- PLAN-187 batch Lessons Queue review remains out of scope unless this release
  proves per-case admission is operationally too slow.
- This patch does not claim executor sandboxing or permission isolation under
  unmanaged ambient authority.
