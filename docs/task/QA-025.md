# QA-025 Case-driven Brain loop release validation

- **status**: in_progress
- **priority**: P1
- **owner**: local
- **createdAt**: 2026-05-09 15:08
- **claimedAt**: 2026-05-09 15:08
- **relatesTo**: FEAT-058, QA-024, REL-031

## Context

QA-024 proved the source-level Case-driven Brain loop behavior after FEAT-058.
This QA record raises the bar to release and production readiness for `0.12.1`.

## Scope

- Source release gates.
- Dist artifact and publish dry-run.
- Source dogfood using a real project-scoped worker state.
- Published-package smoke after release.
- Published-package compact governance harness.

## Evidence

- Focused regression after dogfood-discovered bug:
  - `bun test packages/shared/src/brain/admission.test.ts` passed and now
    covers `authorityMode` / `authorId` / `authHint` visibility under default
    redaction.
  - `bun test packages/core/src/worker/brain/cases/service.test.ts` passed.
  - `bun test apps/cli/src/commands/worker/case.test.ts` passed.
- Full source gates after the fix:
  - `bun run check` passed.
  - `bun run test` passed.
  - `bun run build` passed.
  - `git diff --check` passed.
- Dist and package checks:
  - `bun apps/cli/dist/aiworker-bun.js --version` -> `aiworker/0.12.1
    darwin-arm64 node-v24.3.0`.
  - `bun pm pkg get version --cwd apps/cli/dist` -> `0.12.1`.
  - `bun publish --dry-run --access public` from `apps/cli/dist` packed 34
    files / 3.20MB and stopped only at local npm authentication.
- Dogfood result:
  - Root:
    `/private/tmp/aiworker-release-0.12.1-dogfood-BO6vss`.
  - Worker id: `w_r495zkvm6dz9`.
  - Case statuses:
    `task-release-heuristic=needs_review`,
    `task-release-reviewed=ready_to_ship`,
    `task-release-risk=needs_review`.
  - `task-release-reviewed` selected
    `Reviewed release answer. This is the second final answer.` and only
    counted its own 2 Journal events inside a shared conversation.
  - `task-release-risk` kept `authorityMode=unmanaged_ambient`,
    `risk=high`, `enforceable=false`, and the operator recommendation
    `switch to plan-only`.
  - `lessons propose task-release-reviewed` created pending proposal
    `inbox-task-release-reviewed-1-43890fb3b497`.

## Conclusion

- Source and dist release readiness are satisfied for tagging `v0.12.1`.
- Final production-readiness conclusion is still pending remote release
  workflow, npm install smoke, GitHub Release asset verification, and
  published-package compact governance harness.
