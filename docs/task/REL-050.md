# REL-050 发布 aiworker CLI 0.18.4

- **status**: completed
- **priority**: P0
- **owner**: codex
- **createdAt**: 2026-05-19
- **claimedAt**: 2026-05-19
- **plan**: PLAN-375
- **relatesTo**: v0.18.3, BUG-139, PLAN-374, b47ecaa9

## Background

`v0.18.3` is the current npm latest and GitHub Release. Current `main` contains
the following commits after `v0.18.3`:

- `6900431a` updates the Worker Web bundle size baseline after the previous
  release exposed stale lint metadata.
- `8fbfec15` records the `0.18.3` release evidence.
- `46cb72cc` refines HR recent session entries.
- `b47ecaa9` fixes the HR profile composer select expanded menu styling.

This patch release publishes the current Worker Web and shared component
improvements as `@zonease/aiworker-cli@0.18.4`.

## Scope

- Bump CLI package version from `0.18.3` to `0.18.4`.
- Run local release gates covering check/test/build, mounted-surface smoke,
  dist package checks, pack dry-run, dist release smoke, UI governance, and
  code-review-graph.
- Push `main`, create and push annotated tag `v0.18.4`.
- Monitor the tag-triggered GitHub Actions release workflow and main lint
  workflow.
- Verify npm latest, explicit `bunx` version, GitHub Release assets, and
  published-package smoke.
- Record release evidence and leave the worktree clean.

## Non-goals

- Do not add new product changes during release prep.
- Do not rewrite release workflow or backfill older release docs.
- Do not publish without release workflow success.

## Acceptance

1. Local release gates pass.
2. `apps/cli/package.json` and `apps/cli/dist/package.json` report `0.18.4`.
3. `main` and `v0.18.4` are pushed.
4. GitHub Actions release workflow succeeds.
5. npm latest resolves to `0.18.4`.
6. `bunx @zonease/aiworker-cli@0.18.4 --version` reports `aiworker/0.18.4`.
7. GitHub Release `v0.18.4` exists with four binary tarballs and four matching
   `.sha256` assets.
8. Published-package smoke proves the released CLI starts Host Web/API,
   bootstraps official HR/QA Soul Apps, and invokes HR/QA mounted actions.
9. Local worktree is clean.

## Record

- 2026-05-19 18:05 CST: User requested conventional commit, push, and release.
  Confirmed npm latest is `0.18.3`, latest tag is `v0.18.3`, and current `main`
  contains four commits after the tag. Started `0.18.4` patch release prep.
- 2026-05-19 18:18 CST: Local release gates passed through build,
  mounted-surface smoke, dist version/package checks, pack dry-run,
  dist release smoke, and code-review-graph. Preparing the release commit and
  `v0.18.4` tag.
- 2026-05-19 18:23 CST: Release completed. `main` and annotated tag `v0.18.4`
  were pushed; release workflow `26090536203` and main lint workflow
  `26090531497` passed. npm latest resolves to `0.18.4`; explicit
  `bunx @zonease/aiworker-cli@0.18.4 --version` reports `aiworker/0.18.4`;
  GitHub Release `v0.18.4` is published with 8 assets; published-package smoke
  passed from `/tmp/aiworker-release-0.18.4-published-smoke-AwGwV9/report.json`.

## Verification

- `bun run check` passed.
- `bun run test` passed.
- `bun run build` passed.
- `bun run web:smoke:mounted-surfaces` passed with HR mounted surface,
  settings, and descriptor checks.
- `git diff --check` passed.
- `bun apps/cli/dist/aiworker-bun.js --version` reported
  `aiworker/0.18.4 darwin-arm64 node-v24.3.0`.
- `jq -r '.name + "@" + .version' apps/cli/dist/package.json` reported
  `@zonease/aiworker-cli@0.18.4`.
- `cd apps/cli/dist && npm pack --dry-run --json` produced
  `zonease-aiworker-cli-0.18.4.tgz` with 146 entries.
- `bun run --filter '@zonease/aiworker-cli' smoke:dist-release` passed.
- `bun run crg:update` passed.
- `bun run crg:review` passed with risk score `0.00`.
- Release workflow `26090536203` passed.
- Main lint workflow `26090531497` passed.
- `npm view @zonease/aiworker-cli version dist-tags --json` reports
  `{"version":"0.18.4","dist-tags":{"latest":"0.18.4"}}`.
- `bunx @zonease/aiworker-cli@0.18.4 --version` reported
  `aiworker/0.18.4 darwin-arm64 node-v24.3.0`.
- `gh release view v0.18.4 --repo ZonEaseTech/aiworker --json
  tagName,isDraft,isPrerelease,assets,url,publishedAt,targetCommitish`
  reported a formal non-draft, non-prerelease release published at
  `2026-05-19T10:10:44Z` with 8 assets.
- Published-package smoke passed against `bunx @zonease/aiworker-cli@0.18.4`,
  covering daemon health, runtimeVersion `0.18.4`, Worker Web HTML and
  referenced asset serving, official HR/QA app availability, HR template
  listing, and HR/QA mounted action invocation.
