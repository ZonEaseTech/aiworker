# BUG-105 未安装 engine icon 弱化后不可见

- **status**: completed
- **priority**: P0
- **owner**: local
- **createdAt**: 2026-05-11 20:34
- **plan**: PLAN-263
- **relatesTo**: apps/web

## 背景

Settings engine card 已经为未安装 engine 渲染本地 SVG mask，但 muted 状态把 icon
颜色和背景都设为同一个灰色，再叠加 disabled 卡片透明度后，视觉上等同未渲染。

## 目标

未安装 engine 仍显示专属 icon，并保持禁用/弱化的视觉状态。

## 验收标准

- 未安装 engine 的 card 中仍存在对应 `.agent-icon-shape`。
- muted icon tile 的前景和背景有明确对比度。
- 不改变 engine 选择、测试、重扫和 autosave 行为。
- 更新测试、浏览器验证和 code-review-graph 审查。

## 验证

- Passed: `bun run --filter '@zonease/aiworker-web' typecheck`
- Passed: `bun run --filter '@zonease/aiworker-web' lint`
- Passed: `bun run --filter '@zonease/aiworker-web' test`
- Passed: `bun run --filter '@zonease/aiworker-web' build`
- Passed: `bun run check`
- Passed: `git diff --check`
- Passed: Browser verification on `http://127.0.0.1:9217/`: all disabled engine icons keep
  mask assets and foreground/background contrast.
- Passed: `bun run crg:update`
- Passed: `bun run crg:review`
- Passed: code-review-graph MCP review: low risk, 0 affected flows, 0 test gaps.
