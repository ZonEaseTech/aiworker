# DOC-001 刷新 AGENTS.md 工作指引

- **status**: completed
- **priority**: P3
- **owner**: Codex
- **createdAt**: 2026-04-29 16:44

## 描述

审查并刷新根目录 `AGENTS.md`，使其更符合社区实践：简短、可执行、可验证，保留 AIWorker 必要的不变量和安全边界，移除或下沉过期、重复、环境特定或容易失效的约束声明。

验收标准：

1. `AGENTS.md` 明确列出常用安装、构建、测试、运行与聚焦验证命令。
2. 保留项目关键安全/架构不变量，但避免复制 `docs/architecture.md`、`docs/deployment.md` 等长文档内容。
3. 移除或改写已过期声明，例如 executor 只描述 OpenAI 兼容、测试服专属部署限制内嵌在根指引等。
4. 对复杂约定改为链接权威文档，减少未来 drift。

## 进行时描述

审查并刷新 AGENTS.md 工作指引

## 依赖

- **blocked by**: (无)
- **blocks**: (无)

## 笔记

- 2026-04-29 16:44：已完成初步调查。社区参考显示 AGENTS.md 应作为面向 coding agent 的简短工作说明，覆盖项目概览、构建/测试命令、代码风格、测试指令、安全注意事项，并尽量避免重复 README/架构文档。
- 2026-04-29 16:47：已刷新 `AGENTS.md`，补齐常用命令，修正 executor 与 web UI 组件路径等过期表述，将测试服/部署细节下沉为权威文档链接。验证：`git diff --check -- AGENTS.md docs/task/index.md` 与 `git diff --no-index --check -- /dev/null docs/task/DOC-001.md` 通过；未跑 Bun gate（纯文档改动）。
