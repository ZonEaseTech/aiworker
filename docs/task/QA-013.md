# QA-013 Governance Kernel harness — full 5×2 matrix evidence

- **status**: completed
- **priority**: P1
- **owner**: local
- **createdAt**: 2026-05-06 08:25
- **completedAt**: 2026-05-06 09:01
- **plan**: PLAN-130
- **sourceObjective**: `aiworker-validate worker-source-local`
- **relatesTo**: TODO-033, PLAN-130, QA-010, QA-011, QA-012

## Scope

Run the Governance Kernel harness once with the full matrix
(`developer`, `hr-recruiting`, `finance-ops`, `qa-reviewer`,
`general-assistant` × `codex`, `claude-code`) on source-local mode. Goal:
prove the Soul-agnostic claim of the Brain Governance Kernel by exercising
every Soul on both supported executors against the same set of admission /
truthfulness / risk / REST assertions.

## Result

Overall: **PASS**.

Command:

```bash
PATH="$HOME/.bun/bin:$PATH" bun scripts/governance-kernel-harness.ts \
  --mode worker-source-local \
  --matrix full \
  --debug-root /home/ben/projects/debug-aiworker/qa-2026-05-06-governance-full \
  --timeout-ms 240000 \
  --port-base 19540
```

Evidence root:

- `/home/ben/projects/debug-aiworker/qa-2026-05-06-governance-full`

Sanitized report:

- `/home/ben/projects/debug-aiworker/qa-2026-05-06-governance-full/reports/governance-kernel-report.md`

Machine summary:

- `/home/ben/projects/debug-aiworker/qa-2026-05-06-governance-full/reports/governance-kernel-summary.json`

## Pair Matrix

All 10 pairs PASS with **30 / 30 checks PASS** (no fail / no skip):

| Pair | Soul | Executor | Checks | Result |
|------|------|----------|--------|--------|
| `developer-codex` | developer | codex/default | 30P/0F/0S | PASS |
| `developer-claude-code` | developer | claude-code/default | 30P/0F/0S | PASS |
| `hr-recruiting-codex` | hr-recruiting | codex/default | 30P/0F/0S | PASS |
| `hr-recruiting-claude-code` | hr-recruiting | claude-code/default | 30P/0F/0S | PASS |
| `finance-ops-codex` | finance-ops | codex/default | 30P/0F/0S | PASS |
| `finance-ops-claude-code` | finance-ops | claude-code/default | 30P/0F/0S | PASS |
| `qa-reviewer-codex` | qa-reviewer | codex/default | 30P/0F/0S | PASS |
| `qa-reviewer-claude-code` | qa-reviewer | claude-code/default | 30P/0F/0S | PASS |
| `general-assistant-codex` | general-assistant | codex/default | 30P/0F/0S | PASS |
| `general-assistant-claude-code` | general-assistant | claude-code/default | 30P/0F/0S | PASS |

Total: **300 source-backed checks PASS, 0 fail, 0 skipped**.

## Evidence Highlights

- Every Soul on both executors performs the full chat-id continuity sequence
  (1 conversation row, 12 messages per pair).
- Every Soul on both executors performs the positive admission roundtrip
  (`pending → approved → applied`), writes exactly one canonical memory file
  under `memories/harness-<pair>.md`, appends the MEMORY.md index entry,
  records `decision='applied'`, and the brief projection picks up the new
  memory.
- Every Soul on both executors performs the reject negative path
  (`pending → rejected`), with no canonical memory file leakage.
- Every Soul on both executors blocks the secret-bearing fixture at apply
  (`outcome.kind='blocked-by-secret-scan'`), keeps proposal `approved`, and
  writes no `applied` decision row or memory file.
- Every Soul on both executors emits truthful `source` / `mode` / `evaluator`
  fields on `orchestrator.intent_decision`,
  `orchestrator.capability_decision`, `orchestrator.quality_gate`.
- Every pair surfaces `risk=high` for the high-risk prompt (turn 4).
- Every pair completes the REST/SSE smoke (auth boundary, brain summary,
  OpenAPI path count, SSE connect, Worker Admin mount).

Tool-call observability per pair (parity baseline; no claim of equality):

| Pair | tool_call events |
|---|---|
| developer-codex | 30 (24 LLM-turn + 6 admission/CLI) |
| developer-claude-code | 6 |
| hr-recruiting-codex | 44 |
| hr-recruiting-claude-code | 4 |
| finance-ops-codex | 26 |
| finance-ops-claude-code | 6 |
| qa-reviewer-codex | 34 |
| qa-reviewer-claude-code | 7 |
| general-assistant-codex | 28 |
| general-assistant-claude-code | 7 |

Codex emits significantly more `orchestrator.tool_call` events than
Claude Code in every Soul, consistent with the executor parity contract:
Codex's app-server protocol surfaces every shell / file tool turn; Claude
Code's adapter normalizes streamed responses and emits one event per
distinct tool invocation. The harness asserts non-zero on Codex and accepts
emitted-or-not on Claude Code per the documented executor parity boundary.

## Soul-Agnostic Claim

The Brain Governance Kernel must hold the same invariants regardless of
Soul. This run is the strongest source-backed evidence of that claim to
date: 5 distinct Souls × 2 distinct executors × 30 source-backed checks per
pair, all PASS, zero divergence. The Soul module surfaces only act through
documented hooks (persona, risk preferences, vocabulary, brief sections);
they do not change admission state-machine behavior, secret defense,
truthfulness fields, REST auth, or the canonical memory boundary.

## Non-Scope

- Did not exercise `--allow-secret-body redact` or `--allow-secret-body raw`
  paths in the harness (covered by service-layer unit tests).
- Did not exercise rollback-after-apply (not implemented in materializer).
- Did not run the full matrix against `cli-release-local`. QA-009 / QA-011 /
  QA-012 establish that the published 0.9.1 CLI matches source on the
  compact matrix; broader published-side parity is left as a follow-up if a
  divergence ever surfaces.
- Did not assert tool-call event counts as equality between executors;
  parity here is a non-zero / observable contract, not a fixed-count match.

## Notes

- 2026-05-06 08:25: Opened to close the Soul-agnostic kernel claim with
  source-backed evidence on every Soul × executor pair.
- 2026-05-06 09:01: Completed. Full 5×2 matrix produced 300 PASS / 0 FAIL /
  0 SKIPPED on source-local mode. The Brain Governance Kernel claim is
  now backed by every Soul on every supported executor.
