# QA-009 Run Governance Kernel regression harness against CLI 0.9.1

- **status**: completed
- **priority**: P1
- **owner**: local
- **createdAt**: 2026-05-06 06:47
- **completedAt**: 2026-05-06 06:47
- **plan**: PLAN-127
- **sourceObjective**: `TODO-027 Create Governance Kernel regression harness`
- **relatesTo**: TODO-027, REL-017, QA-008

## Scope

Run the new repeatable Governance Kernel harness in `cli-release-local` mode
against the already published `@zonease/aiworker-cli@0.9.1` package.

Command:

```bash
PATH="$HOME/.bun/bin:$PATH" bun scripts/governance-kernel-harness.ts \
  --mode cli-release-local \
  --version 0.9.1 \
  --matrix compact \
  --debug-root /home/ben/projects/debug-aiworker/qa-2026-05-06-governance-harness-0.9.1-r2 \
  --timeout-ms 240000 \
  --port-base 19450
```

Evidence root:

- `/home/ben/projects/debug-aiworker/qa-2026-05-06-governance-harness-0.9.1-r2`

Sanitized report:

- `/home/ben/projects/debug-aiworker/qa-2026-05-06-governance-harness-0.9.1-r2/reports/governance-kernel-report.md`

Machine summary:

- `/home/ben/projects/debug-aiworker/qa-2026-05-06-governance-harness-0.9.1-r2/reports/governance-kernel-summary.json`

## Result

Overall: **PASS**.

Executor availability:

- `codex`: PASS, `codex-cli 0.128.0`.
- `claude-code`: PASS, `2.1.129 (Claude Code)`.

Matrix:

| Pair | Soul | Executor | Result |
|------|------|----------|--------|
| `developer-codex` | developer | codex/default | PASS |
| `general-assistant-claude-code` | general-assistant | claude-code/default | PASS |

## Evidence Summary

- Both pairs initialized fresh project scopes, selected the requested executor,
  and passed `executor doctor`, `doctor`, and `brain status`.
- Both pairs completed six same `chat-id` CLI turns with
  `orchestrator.finished`.
- `worker.db` showed exactly one conversation row per pair and 12 messages per
  chat id.
- Decision events exposed truthful source/mode/evaluator fields:
  18 decision events per pair, and persisted decision samples were present in
  worker.db (`developer-codex`: 12 rows; `general-assistant-claude-code`: 17
  rows).
- Admission behavior was source-backed:
  the LLM turn created pending `memory-add` proposal rows, the direct formal
  admission path also created a pending proposal, and no canonical memory file
  was written directly.
- Tool-call observability was present:
  `developer-codex` emitted 28 tool-call events; `general-assistant-claude-code`
  emitted 7.
- Risk-policy behavior emitted `risk=high` for the high-risk prompt in both
  pairs.
- REST/SSE smoke passed for both pairs:
  `/health=200`, authenticated `/api/worker/info=200`, unauthenticated and bad
  bearer `/api/worker/info=401`, `/api/worker/brain/summary=200`,
  OpenAPI path count `14`, SSE connected, and `/admin/=200`.

## Non-Scope

- Did not attach to a shared fleet, gateway, enrollment flow, Fleet UI, or
  remote worker.
- Did not run the full 5 Soul x 2 executor matrix; the harness supports
  `--matrix full`, but this QA run used the compact default required to prove
  repeatability.
- Did not publish a new package; this validated the existing 0.9.1 published
  CLI and the repository harness script.

## Notes

- First local attempt failed because the harness incorrectly set
  `AIWORKER_HOME` for project-scope `init --soul`; the script was fixed to
  preserve project-scope semantics.
- Second local attempt failed because the harness direct admission fixture used
  invalid evidence shape; the product LLM path still created valid pending
  proposal rows. The fixture was corrected to use `kind=observation`, `at`, and
  `ref`.
- The final `r2` run passed with no skipped or failed checks.
