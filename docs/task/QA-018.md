# QA-018 Governance Kernel harness — brain-skill-add roundtrip evidence

- **status**: completed
- **priority**: P1
- **owner**: local
- **createdAt**: 2026-05-07 11:34
- **completedAt**: 2026-05-07 11:34
- **relatesTo**: TODO-038, PLAN-156, REFACTOR-022, QA-011, QA-012

## Objective

Record source-local black-box evidence that approved `brain-skill-add`
proposals can be materialized through the same governed admission boundary as
memory proposals.

## Evidence

Command:

```bash
PATH="$HOME/.bun/bin:$PATH" bun scripts/governance-kernel-harness.ts \
  --mode worker-source-local \
  --matrix compact \
  --debug-root /Users/ben/projects/aiworker/tmp/governance-brain-skill-20260507-1123 \
  --port-base 19830 \
  --timeout-ms 240000
```

Result:

- Overall: **PASS**
- Total checks: **80 PASS / 0 FAIL / 0 SKIPPED**
- Pairs:
  - `developer-codex`: PASS
  - `general-assistant-claude-code`: PASS

Machine evidence:

- `/Users/ben/projects/aiworker/tmp/governance-brain-skill-20260507-1123/reports/governance-kernel-report.md`
- `/Users/ben/projects/aiworker/tmp/governance-brain-skill-20260507-1123/reports/governance-kernel-summary.json`

## New Checks

For each compact pair, the harness now verifies:

1. `brain-skill-add admission apply commit`: propose / approve / apply all exit
   successfully and `outcome.kind=applied`.
2. `post-apply canonical Brain Skill file`: the expected
   `.aiworker/skills/<skillId>/SKILL.md` exists and contains the expected
   frontmatter/body markers.
3. `brain-skill-add DB transitions`: proposal status is `applied` and
   decisions include `approved,applied`.
4. `doctor accepts applied Brain Skill`: `aiworker doctor` still exits 0 after
   the new Brain Skill is materialized.

## Interpretation

This extends the positive Project Brain admission invariant from memory-only to
skill-capable self-iteration. Durable generated Brain Skills can now be proven
through a repeatable black-box source harness:

`brain admission propose → approve → apply --commit → canonical SKILL.md → DB
applied decision → doctor accepts the resulting Project Brain`.

The run also revalidated the existing memory-add roundtrip, reject path,
secret-scan-block path, conversation continuity, decision truthfulness,
tool-call observability, REST auth boundary, Worker Admin mount, and serve
restart continuity checks.

## Notes

- 2026-05-07 11:34: Source-local compact run passed with both available
  executors. Published CLI validation remains a release-specific follow-up,
  not part of this source implementation slice.
